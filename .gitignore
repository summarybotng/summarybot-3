.claude
.claude-flow

# Rust
/target
**/*.rs.bk

# ruflo auto-generated CLAUDE.md backups (local tooling artifacts)
CLAUDE.md.pre-ruflo*

# Web dashboard (node)
web/node_modules
web/dist

# Local secrets / env — never commit (only the *.example template is tracked)
.env
.env.local
.env.*.local

# Local SQLite databases (demo/dev runtime data + WAL/SHM sidecars)
*.db
*.db-wal
*.db-shm

# WhatsApp exports dropped in for local testing — private chat data, never commit
WhatsApp Chat*.zip
*_chat.txt
