# v2 → v3 UX parity gap matrix
> Auto-generated (ADR-134 §4 method) by a 24-agent source-diff sweep: each v2 page
> (`/workspaces/summarybot-ng-reference/src/frontend/src/pages/`) compared against its
> v3 counterpart (`web/src/views/`), every gap classified against the ADRs. Regenerate
> by re-running the `v2-v3-parity-gap` workflow. **Triage `regression` + `unknown`
> high/medium rows first; `intentional`/`superseded` are recorded for completeness.**

**290 gaps** across **24 pages** — 🟢 115 intentional, 🟠 93 unknown, 🔵 51 superseded, 🔴 31 regression · by severity: 10 high, 91 medium, 189 low

## Priority backlog — 67 regression/unknown gaps (high + medium)

| Sev | Page→v3 | Element | What v2 does | Fix / note |
|---|---|---|---|---|
| high | Archive→CreateSummary (Past flow) + Coverage + Jobs + Whatsapp + Spend (Archive is split across these; no single 1:1 view) | Pre-generation cost estimate (Estimate button → periods/tokens/cost/model) | GenerateDialog has an Estimate button calling onEstimate(buildRequest(true)) (dry_run) and renders periods, estimated_tokens, estimated_cost_usd, model before committing. Archive.t | CreateSummary's Past flow (CreateSummary.tsx:106-110) calls summarizeWeeks with no dry-run/estimate step. No v3 view exposes a pre-flight cost estimate for retrospective generation |
| high | Channels→Source (web/src/views/Source.tsx) | Locked/restricted channel governance (🔒 badges, confirm dialog, audit warnings) | Channels with is_locked are disabled-by-default, visually flagged with Lock icon + 'Restricted', enabling one triggers a window.confirm warning that the action is logged for audit, | No is_locked/locked_override concept in v3 web (grep: zero hits). v3 Source only distinguishes accessible vs inaccessible (Source.tsx:342-357). ADR-133 lists Channels parity work a |
| high | Feeds→Feeds (/workspaces/summarybot-3/web/src/views/Feeds.tsx) | Edit feed flow (Edit dialog + per-card Edit action) | v2 has a full Edit dialog reusing FeedForm and a per-card pencil Edit button; openEditDialog pre-fills form from the feed including criteria (Feeds.tsx:87-100, 257-297; FeedCard.ts | No edit affordance in v3 — feeds can only be created and deleted. Restore an edit dialog (title/description/is_public/max_items/criteria) backed by an updateFeed mutation. |
| high | Feeds→Feeds (/workspaces/summarybot-3/web/src/views/Feeds.tsx) | Filter criteria subsystem (ADR-037) on create/edit | v2 FeedForm exposes a collapsible Filter Criteria section (FilterCriteriaForm + FilterCriteriaSummary, active-count badge) covering source/channels/platform/perspective/grounding/k | Entire feed-content filtering capability absent in v3; only a single optional channel_id is supported. ADR-133 A4 spec (line 92) does not list criteria, so this is an undocumented  |
| high | Feeds→Feeds (/workspaces/summarybot-3/web/src/views/Feeds.tsx) | Regenerate token action (private feeds) | v2 offers a per-card RefreshCw Regenerate Token action (private feeds only) guarded by an AlertDialog warning that old URLs stop working; copies new URL to clipboard (FeedCard.tsx: | ADR-133 explicitly lists `Regenerate` as a shipped affordance for feeds (line 11). No regenerate action in v3 Feeds.tsx — users cannot rotate a leaked token. Add a regenerateToken  |
| high | ShareReceived→(none) — closest is web/src/views/Whatsapp.tsx, but it is a manual file-upload form, not the OS share-target entry point | PWA OS-share-target entry path (receive a file shared from WhatsApp/another app and land on a dedicated import page) | ShareReceived is a dedicated route that reads a file stashed by a service worker in IndexedDB ('SummaryBotShare' store, 'sharedFiles'/'pending' key) and drives the import; ShareRec | No PWA in v3: no web manifest, no service worker, no share-target handler, no route. grep for share_target/serviceWorker/SummaryBotShare in web/src finds nothing; App.tsx only rout |
| high | SlackWorkspaces→Source (rendered with platform="slack"; App.tsx:189) | Per-channel auto_summarize and is_sensitive toggles | Channel list lets a user toggle per-channel auto_summarize and is_sensitive (mark sensitive to exclude from cross-channel summaries) via Switches calling onUpdateChannel (SlackChan | v3 SourceChannel (types.ts:514-523) has no auto_summarize/is_sensitive fields and the channel browser (Source.tsx:330-368) is select-to-sync only. Auto-summarize is configured via  |
| high | Tenants→Split across Members.tsx (members/admins/invites) + Settings.tsx (tenant provisioning/selection, LLM config, budget); branding accent only auto-applied in App.tsx | Access Mode setting (public / authenticated / members_only / workspace_members) + allowed_email_domains | Settings tab Access Mode Select with 4 modes plus a comma-separated Allowed Email Domains input gating 'authenticated' mode, with inline help copy (Tenants.tsx:675-713; access_mode | No v3 view exposes access_mode or allowed_email_domains. The v3 Tenant type (types.ts:11-16) doesn't even carry these fields. Not listed as a deferral in ADR-133 (which only claims |
| high | WhatsAppImports→Whatsapp | Participant identity management: rename (set preferred name) and merge | ParticipantList lists per-chat participants with message_count and alias_count, a 'Set name' inline editor (updateParticipantName) and a Merge mode with no-silent-merge confirmatio | ADR-121 §3 (lines 105-108, 176-180) mandates participant claim/merge with 'no silent merge', reversible and audit-logged — a core ADR-121 requirement (WSP-010). v3 exposes no parti |
| medium | AdminAudit→Audit | Summary KPI cards (Total Events, Logins, Failed Actions, Security Alerts, Unique Users) | Five summary cards driven by GET /admin/audit/summary showing total_count, login success count, failed_count, alert_count, and unique-user count (AdminAudit.tsx:204-269); alert bad | No summary endpoint or KPI strip in v3 Audit.tsx (only listAudit). Not cited in ADR-133/134. Restore: add /audit/summary aggregation + card grid; or document as intentional given w |
| medium | AdminAudit→Audit | Event-type text filter (e.g. auth.*) | Free-text Input filtering by event_type wildcard, resets offset on change (AdminAudit.tsx:286-291). | v3 has zero filters; entire list shown unfiltered. Restore by passing event_type to listAudit / backend query param. |
| medium | AdminAudit→Audit | Category filter (Auth/Access/Action/Source/Admin/System) | Select with six audit categories, resets offset (AdminAudit.tsx:300-321, categories list 48-55). | v3 AuditEntry has no category field (types.ts:465-470) and no category filter. Not cited in ADRs. |
| medium | AdminAudit→Audit | Severity filter (Debug/Info/Notice/Warning/Alert) | Select filtering by severity; severities defined with badge colors (AdminAudit.tsx:57-63, 323-344). | v3 AuditEntry has no severity field; no severity filter/column. Not cited in ADRs. |
| medium | AdminAudit→Audit | Pagination (prev/next, showing X-Y of N) | limit/offset pagination with Previous/Next buttons (disabled at bounds) and a 'Showing N-M of total' counter (AdminAudit.tsx:127-141, 461-486). | v3 fetches a flat first-100 (Audit.tsx:33, listAudit(100)) with no paging and no total count. For high-volume tenants older events are unreachable. Not cited in ADRs. |
| medium | AdminAudit→Audit | Tabular columns: Timestamp, Event, User, Guild, Category, Status, Severity | Seven-column table with resource_type/resource_id sub-line, user icon/name vs System, truncated guild id, category badge, success icon, severity badge (AdminAudit.tsx:397-457). | v3 renders a simple list with only action label, timestamp, detail, action code, and optional actor (Audit.tsx:62-78). No resource_type/resource_id, category, status, or severity s |
| medium | AdminAudit→Audit | Entry detail dialog (click row to expand) | Clicking a row opens a Dialog showing event type, full timestamp, user+id, guild, category, status, error_message block, and a JSON details pretty-print (AdminAudit.tsx:411-414, 49 | v3 rows are non-interactive; no detail view, no error_message or structured details payload. Restore would require backend to expose details/error_message on AuditEntry. |
| medium | AdminIssues→Issues.tsx | Status filter dropdown (All Status / Open / Triaged / On GitHub / Closed) | Select control filtering by status, resets offset on change (AdminIssues.tsx:254-274) | v3 has no explicit status dropdown; it offers only a binary 'Show resolved' checkbox (Issues.tsx:42,97-104,51 includeResolved). Cannot filter to e.g. only 'investigating' or only ' |
| medium | Archive→CreateSummary (Past flow) + Coverage + Jobs + Whatsapp + Spend (Archive is split across these; no single 1:1 view) | Summary Type selector (Brief/Detailed/Comprehensive → model tier) | GenerateDialog summaryType select brief(Haiku)/detailed(Sonnet)/comprehensive(Sonnet), threaded into summary_type. Archive.tsx:727, 901-913 | CreateSummary's Past flow exposes no summary length/type control (CreateSummary.tsx:299-303 just runs summarizeWeeks). ADR-133 §B notes Summaries/Schedules gain Length/Perspective  |
| medium | Archive→CreateSummary (Past flow) + Coverage + Jobs + Whatsapp + Spend (Archive is split across these; no single 1:1 view) | Perspective selector (General/Developer/Executive/Marketing/Product/Finance/Support) | GenerateDialog perspective select with 7 perspectives, threaded into the generate request. Archive.tsx:728, 914-930 | No perspective picker in CreateSummary's Past flow. ADR-133 §A2 ships Prompts/Perspectives as a tab and §B adds Perspective to Schedules/Summaries, but the retrospective (Past) gen |
| medium | Archive→CreateSummary (Past flow) + Coverage + Jobs + Whatsapp + Spend (Archive is split across these; no single 1:1 view) | Skip-existing / Regenerate-failed / Force-regenerate toggles | Three switches: skip_existing (default on), regenerate_failed (default on), force_regenerate (default off, 'Delete existing and regenerate all'). Archive.tsx:729-731, 933-960 | CreateSummary Past flow has no idempotency controls; summarizeWeeks(chat) handles empty-week skipping server-side but exposes no skip/force/regenerate-failed UI. v3 has bulk-regene |
| medium | Archive→CreateSummary (Past flow) + Coverage + Jobs + Whatsapp + Spend (Archive is split across these; no single 1:1 view) | Max Cost (USD) cap input | Optional max_cost_usd numeric input that bounds the generation spend (request.max_cost_usd = parseFloat). Archive.tsx:732, 774-776, 982-991 | No spend cap on any v3 generation flow (CreateSummary Past has no cost field). Not addressed in ADR-133. Users cannot bound retrospective cost up front. |
| medium | AuditLog→Audit (web/src/views/Audit.tsx) | Summary stat cards (Total Events / Failed Actions / Security Alerts / Unique Users) | Four summary cards driven by useAuditSummary: total_count, failed_count, alert_count, unique users from Object.keys(summary.by_user) (AuditLog.tsx:135-188); plus an alert-count Bad | No equivalent summary/aggregate in v3 Audit.tsx (it only lists entries). Restore: add an audit-summary endpoint + 4 stat cards. Not addressed in ADR-133 §B (Audit not listed for en |
| medium | AuditLog→Audit (web/src/views/Audit.tsx) | User-name text filter | Input bound to filters.user_name, resets offset, narrows entries by actor name (AuditLog.tsx:205-210). | v3 has no filters at all (Audit.tsx:49-87). Restore as a client/server filter on actor. |
| medium | AuditLog→Audit (web/src/views/Audit.tsx) | Event-type filter (e.g. auth.*) | Input bound to filters.event_type with wildcard hint placeholder, resets offset (AuditLog.tsx:212-217). | v3 has no event-type filter. v3 shows the raw action code as a <code> chip (Audit.tsx:73) but cannot filter on it. |
| medium | AuditLog→Audit (web/src/views/Audit.tsx) | Category filter + category taxonomy | Select over 6 categories (auth/access/action/source/admin/system) with 'All Categories' default; resets offset (AuditLog.tsx:46-53,219-240). Each entry shows a category Badge (Audi | v3 AuditEntry has no category field (types.ts:465-470) and no category filter/badge. v3 events are an implicitly narrow set (member/invite/identity) so categorization may be unnece |
| medium | AuditLog→Audit (web/src/views/Audit.tsx) | Severity filter + severity badges | Select over 5 severities (debug/info/notice/warning/alert) with color-coded badges and 'All Severity' default (AuditLog.tsx:55-61,242-263); per-entry severity Badge via getSeverity | v3 AuditEntry has no severity (types.ts:465-470); no severity filter or badge. Distinct from ADR-133 A3 Errors (which carries severity for operational errors, explicitly 'distinct  |
| medium | AuditLog→Audit (web/src/views/Audit.tsx) | Success/failure status filter and indicators | Select All/Success/Failed bound to filters.success (AuditLog.tsx:265-283); per-row CheckCircle (green) / AlertTriangle (red) status icon (AuditLog.tsx:366-372). | v3 AuditEntry has no success field; no status filter or per-row indicator. v3 events appear to be successful-action records only, but rejections like identity.link.collision exist  |
| medium | AuditLog→Audit (web/src/views/Audit.tsx) | Pagination (offset/limit, Showing X-Y of N, Prev/Next) | Server-side paging via filters.limit/offset with handlePrevPage/handleNextPage, disabled-state buttons, and 'Showing N - M of total' counter (AuditLog.tsx:89-103,379-404). | v3 fetches a single fixed page of 100 (api.ts:569 listAudit(100); Audit.tsx:33) with no total, offset, or next/prev. Entries beyond 100 are silently unreachable. Restore: paginated |
| medium | AuditLog→Audit (web/src/views/Audit.tsx) | Per-entry detail dialog | Clicking a row opens a Dialog showing event type, full timestamp (PPpp), user + id, category, status, severity, resource (type/id/name), error_message block, pretty-printed details | v3 rows are static <li>s with no click target or detail view (Audit.tsx:63-77). v3 AuditEntry lacks the underlying fields (resource, error_message, details, duration_ms, severity,  |
| medium | Coverage→Coverage (web/src/views/Coverage.tsx) | Per-channel navigation to filtered summaries | Each channel row is a Link to /guilds/:id/summaries?channel=<id> with a ChevronRight affordance, letting users drill from a channel's coverage into its summaries (Coverage.tsx:224- | v3 channel rows are static <li> with no link (Coverage.tsx v3:97-112). Not mentioned in ADR-133's read-only framing as removed; likely an unrecorded affordance regression. Restore  |
| medium | Coverage→Coverage (web/src/views/Coverage.tsx) | Channel display name (falls back to id) | Channel rows and gap rows show channel_name \|\| channel_id, so users see human-readable '#job-board' names (Coverage.tsx:233, 302; ChannelCoverage.channel_name field 41-42) | v3 ChannelCoverage (types.ts:178-188) has no channel_name; view renders raw channel_id in mono font (Coverage.tsx v3:100). ADR-133 live-evidence (line 61) explicitly cites named ch |
| medium | Feeds→Feeds (/workspaces/summarybot-3/web/src/views/Feeds.tsx) | Channel selector (dropdown of guild channels) | v2 FeedForm renders a Select populated with the guild's text channels plus 'All Channels', with helper copy (FeedForm.tsx:96-120). | v3 replaces the dropdown with a free-text 'Channel id' input (Feeds.tsx:71-76), forcing users to know raw channel IDs and showing the raw id (`#{channel_id}`) rather than a channel |
| medium | Feeds→Feeds (/workspaces/summarybot-3/web/src/views/Feeds.tsx) | Feed Type selector (RSS vs Atom) | v2 FeedForm lets the user choose RSS 2.0 or Atom 1.0 at create time (FeedForm.tsx:123-142); cards show the chosen type badge. | v3 has no feed_type control (Feeds.tsx:30-45 omits it); feeds are implicitly RSS. ADR-133 A4 (line 92) and the view header still name 'RSS/Atom', so Atom output appears unintention |
| medium | Feeds→Feeds (/workspaces/summarybot-3/web/src/views/Feeds.tsx) | Description, Max Items, Include Full Content fields | v2 FeedForm has a Description textarea (FeedForm.tsx:155-165), a Max Items slider 1-100 with live value + helper (FeedForm.tsx:234-250), and an Include Full Content switch with hel | v3 create form only has title/channel/public (Feeds.tsx:64-88). These three feed-shaping fields are absent; ADR-133 A4 schema (line 92) does not mention them, so the drop is undocu |
| medium | Feeds→Feeds (/workspaces/summarybot-3/web/src/views/Feeds.tsx) | Delete confirmation dialog | v2 wraps the delete action in an AlertDialog with a destructive-consequences warning ('Feed readers using this URL will no longer receive updates') before deleting (FeedCard.tsx:20 | v3 deletes immediately on click with no confirmation (Feeds.tsx:47-51, 132-137). Add a confirm step for this irreversible, externally-visible action. |
| medium | Guilds→App.tsx workspace switcher + Overview.tsx + Prompts.tsx (no single Guilds equivalent) | Refresh Servers button (re-fetch server list from Discord) | A 'Refresh Servers' button calls POST /auth/refresh, updates the token, re-fetches guilds, with spinner + success toast and a SESSION_EXPIRED-aware error toast (Guilds.tsx:291-310, | No way in v3 to re-pull the granted workspace set from the identity provider; client.workspaces() is fixed from the session token (App.tsx:120-135). The user must sign out/in to ch |
| medium | JobsTab→Jobs (web/src/views/Jobs.tsx) | Auto-refresh polling for active jobs | Jobs list auto-refreshes every 3s while any job is running/pending (JobsTab.tsx:816-823); stats poll every 10s (Jobs.tsx:60). A blue 'N active job(s) - auto-refreshing' banner is s | v3 loads once on mount and only updates via the manual Refresh button (Jobs.tsx:46-60). Live job progress is invisible without manual reload. Restore: add a refetch interval gated  |
| medium | JobsTab→Jobs (web/src/views/Jobs.tsx) | Progress bar + percent + progress message | Running/pending jobs render a Progress bar with percent, a progress message, and current_period, e.g. 'Processing… (2024-01) 3/10 (30%)' (JobsTab.tsx:289-304). | v3 only shows a bare 'current/total' count inline (Jobs.tsx:171-175) with no percent, no message, no progress bar, no current_period. The v3 Job type has progress_current/progress_ |
| medium | JobsTab→Jobs (web/src/views/Jobs.tsx) | Cancel job action | Cancel button for running/pending/paused jobs hits POST /jobs/:id/cancel with toast feedback (JobsTab.tsx:483-498, 827-847). | v3 exposes only retryJob in the client (api.ts:413-423); no cancel endpoint/button. ADR-133 D1 (line 140) documents pause/resume as N/A-by-architecture (synchronous jobs) but does  |
| medium | JobsTab→Jobs (web/src/views/Jobs.tsx) | Human-readable job title + description | generateJobTitle/generateJobDescription produce titles like 'Daily Summary' and descriptions like 'Discord · server scope · 3 channels · 2024-01-01 - 2024-01-07' (JobsTab.tsx:135-1 | v3 shows only a raw TYPE_LABEL (e.g. 'Scheduled summary') plus scope (Jobs.tsx:167-170); no synthesized title or descriptive subtitle. ADR-133 §B lists enrichment fields but does n |
| medium | JobsTab→Jobs (web/src/views/Jobs.tsx) | Navigable links to produced summaries and schedule | Job cards link to the schedule (/schedules?highlight=) and to produced summaries ('View Summary' / 'View N Summaries' -> /summaries?view=) (JobsTab.tsx:409-443). | v3 renders schedule_name as plain mono text and summary_ids as a comma-joined mono string with no links (Jobs.tsx:194-242). Users cannot navigate from a job to its output or schedu |
| medium | Populate→Knowledge.tsx + RuVector.tsx (Populate-as-screen deferred per ADR-133 A8) | Ingestion-path settings: Wiki Pages auto-ingest toggle + RuVector Knowledge Units toggle (wiki_auto_ingest / wiki_ingest_to_vectors) | Populate.tsx:360-398 — two Switches PATCHing /wiki/settings {wiki_auto_ingest}, {wiki_ingest_to_vectors}; controls what gets populated when new summaries are created | No grep hit for wiki_auto_ingest/ingest_to_vectors anywhere in web/src. ADR-133 A8 does not enumerate these per-path ingestion toggles as covered; only the backfill trigger is addr |
| medium | PromptTemplates→Prompts.tsx | Description field (create/edit form + card display) | v2 form has an optional Description input (PromptTemplates.tsx:369-379) persisted via based_on/description and shown on each card (PromptTemplates.tsx:541-545). | v3 editor only has name+content (Prompts.tsx:109-121) and the v3 PromptTemplate type has no description field (types.ts:209-217). Restore: add description to type, API create/updat |
| medium | PromptTemplates→Prompts.tsx | "New template" templates: Start-from-scratch + Based-on-default seeding | v2 New Template is a dropdown with 'Start from scratch' plus one item per built-in default ('Based on: {name}'), seeding name/content from the chosen default (PromptTemplates.tsx:1 | v3 'New template' is a single button opening a blank editor (Prompts.tsx:76-82). The v3 API createPrompt already accepts based_on (api.ts:510) and the type tracks based_on (types.t |
| medium | PromptTemplates→Prompts.tsx | Delete confirmation with in-use warning + force delete | v2 delete AlertDialog shows a distinct message when usage_count>0 ('used by N schedule(s)… will remove the template reference') and forces force=true on delete (PromptTemplates.tsx | v3 remove() deletes immediately with no confirmation dialog and no in-use warning (Prompts.tsx:59-63,160-165). Even an unconfirmed delete of an in-use template is a destructive-act |
| medium | RuVectorExplorer→RuVector (web/src/views/RuVector.tsx) — semantic search + RVF export live in the Knowledge view (web/src/views/Knowledge.tsx) | Unit-type filter (claim/decision/question/action_item/context/definition/reference) for browsing units | Both Search and Browse tabs offer a Select to filter units by type, querying the backend with unit_type/unit_types (RuVectorExplorer.tsx:344-358, :422-436, :108-122). | v3 RuVector lists units with their kind shown (RuVector.tsx:71) but offers no filter by type; client.vectors() takes no type param. Knowledge view also has no unit-type filter. No  |
| medium | RuVectorExplorer→RuVector (web/src/views/RuVector.tsx) — semantic search + RVF export live in the Knowledge view (web/src/views/Knowledge.tsx) | Result limit / page-size selector (25/50/100/200) for browsing units | Browse tab Select sets the fetch limit and shows 'Showing N of total' (RuVectorExplorer.tsx:437-447, :486-490). | v3 RuVector renders all units returned by client.vectors() with no limit control or 'showing N of total' affordance (RuVector.tsx:66-86). No pagination at all; risks unbounded list |
| medium | ScheduleCard→Schedules (web/src/views/Schedules.tsx) | Per-schedule run/failure/last-run statistics on the schedule card | ScheduleCard shows "Runs: {run_count}", "Failures: {failure_count}", "Last: {relative last_run}", and "Next: {relative next_run}" (ScheduleCard.tsx:197-210) | v3 list shows only enabled/paused and next-run absolute time (Schedules.tsx:292-308). v3 Schedule type lacks run_count/failure_count/last_run (it has consecutive_failures and next_ |
| medium | ScheduleCard→Schedules (web/src/views/Schedules.tsx) | Missed-run detection + prominent "Catch Up" action | When next_run is in the past and active, ScheduleCard shows a destructive "Missed" badge and a prominent orange "Catch Up" button instead of the plain Run-now (ScheduleCard.tsx:105 | v3 always shows a plain "Run now" button (Schedules.tsx:311-316) and never flags missed schedules. v3 has next_run available to compute this. Not addressed in ADR-133/134. |
| medium | ScheduleCard→Schedules (web/src/views/Schedules.tsx) | Delete confirmation dialog | Delete is gated behind an AlertDialog confirming "Are you sure you want to delete '{name}'? This action cannot be undone." (ScheduleCard.tsx:346-370) | v3 deletes immediately on clicking the 🗑 button with no confirmation (Schedules.tsx:329-334). Add a confirm step to prevent accidental destructive action. |
| medium | ScheduleCard→Schedules (web/src/views/Schedules.tsx) | Per-run error/detail text in run history | RunHistoryDrawer renders execution.error in red and a computed run duration per execution (RunHistoryDrawer.tsx:140-155) | v3 inline run list shows only status, (manual), and ran_at (Schedules.tsx:344-352) and never renders ScheduleRun.detail, which carries the failure/info message (types.ts:114). Rend |
| medium | Summaries→Summaries.tsx (list/compose/filters/facets/calendar); generation wizard split into CreateSummary.tsx | Created date-range filter with presets | Date-range popover (created_after/before) with Today/7d/30d/90d presets and dual calendars (SummaryFilters.tsx:430-474,1137-1216) | v3 only offers free-text q/participant/tag/archived (Summaries.tsx:169-228). No date filtering at all on the list; the Calendar view shows only the month of the latest summary. Res |
| medium | Summaries→Summaries.tsx (list/compose/filters/facets/calendar); generation wizard split into CreateSummary.tsx | Server-side pagination | Paginated list (20/page) with Prev/Next and 'Page X of Y' (StoredSummariesTab.tsx:356,391-393,753,992-1010) | v3 listSummaries takes no page/limit/offset (api.ts:213-223) and loads the whole set, filtering client-side (Summaries.tsx:140-144). Scales poorly for large workspaces; v2 paginate |
| medium | Summaries→Summaries.tsx (list/compose/filters/facets/calendar); generation wizard split into CreateSummary.tsx | Bulk Delete over selection | BulkActionBar Delete(N) with AlertDialog confirmation (BulkActionBar.tsx:246-255,285-314) | v3 offers only per-row delete (Summaries.tsx:371-377); no multi-select bulk delete. Not addressed in ADR-133 §D. Deleting many summaries is one-at-a-time. |
| medium | Summaries→Summaries.tsx (list/compose/filters/facets/calendar); generation wizard split into CreateSummary.tsx | Generation wizard: Summary Length (Brief/Detailed/Comprehensive) | Length select feeding summary_length, also drives model selection (Summaries.tsx legacy dialog :956-968; wizard threads summaryLength :219-222) | v3 CreateSummary (now/recurring/past) has no length/perspective/options step — submit() sends only channel + time window (CreateSummary.tsx:81-116). Compose box in v3 Summaries als |
| medium | Summaries→Summaries.tsx (list/compose/filters/facets/calendar); generation wizard split into CreateSummary.tsx | Generation wizard: Perspective + custom prompt template selection | Perspective select incl. custom templates, threaded as perspective/prompt_template_id into generate (Summaries.tsx legacy :970-1016; wizard :218,227) | v3 CreateSummary does not let the user pick a perspective or prompt template when generating (CreateSummary.tsx:81-116). Prompts exist as a separate screen (ADR-133 A2) but are not |
| medium | Tenants→Split across Members.tsx (members/admins/invites) + Settings.tsx (tenant provisioning/selection, LLM config, budget); branding accent only auto-applied in App.tsx | Custom domain verification flow (TXT + CNAME records, verify button, status badges) | TenantDomainTab: set custom domain, initiate verification, show DNS TXT verification_record + CNAME target with copy-to-clipboard, a 'Verify Domain' action, plus default-subdomain/ | v3 keeps subdomain/custom_domain on the Tenant type (types.ts:14-15) and reads them for branding resolution (App.tsx:99-104) but offers no UI to set or verify a custom domain. Not  |
| medium | Tenants→Split across Members.tsx (members/admins/invites) + Settings.tsx (tenant provisioning/selection, LLM config, budget); branding accent only auto-applied in App.tsx | Link Workspace tab (attach Discord guild to tenant, list/unlink linked workspaces) | TenantWorkspacesTab: select an available (not-yet-linked) Discord guild, optional display name, Link Workspace action, and a linked-workspaces list with guild icons and per-row unl | No v3 view links workspaces/guilds to a tenant. ADR-133 claims Tenancy as a v3 strength but lists no workspace-linking deferral. The v3 dashboard is workspace-scoped (Members.tsx:6 |
| medium | Tenants→Split across Members.tsx (members/admins/invites) + Settings.tsx (tenant provisioning/selection, LLM config, budget); branding accent only auto-applied in App.tsx | Delete Tenant danger zone with typed confirmation dialog | TenantDetails Danger Zone card with a destructive 'Delete Tenant' button opening a confirmation Dialog ('This will permanently delete the tenant, unlink all workspaces, and remove  | No v3 view offers tenant deletion. ADR-133 does not record this as a deferral. Settings supports provision/claim but not delete. Restore: DELETE /tenants/:slug + a confirmed danger |
| medium | WhatsAppImports→Whatsapp | Sanitized message viewer with search and pagination | MessageList fetches messages (50/page), renders sender/timestamp/content with system-message and attachment styling, a 'Search messages...' box, and Previous/Next pagination showin | ADR-121:73 explicitly retains 'a sanitized message view (pseudonyms only, never phone numbers)' as part of the model, yet v3 surfaces no message-browsing UI at all. Restore hint: a |
| medium | guild dashboard home)→Overview | "This Week" stat card (summaries_this_week) | Stat card showing guild.stats.summaries_this_week. GuildDashboard.tsx:35-41 | v3 Overview type has no this-week count (types.ts:309-318). ADR-133 A6 lists 'counts (summaries, schedules, members)' but not a weekly count; not explicitly deferred. Restore: add  |
| medium | guild dashboard home)→Overview | "Enabled Channels" stat card | Stat card showing guild.config.enabled_channels.length — config status surfaced on the dashboard. GuildDashboard.tsx:49-55 | ADR-133 A6 explicitly scopes Overview to include 'config status', but v3 surfaces no channel/config count. v3 substitutes Coverage% (Overview.tsx:53). Restore: add an enabled-chann |
| medium | guild dashboard home)→Overview | Primary "Generate Summary" CTA button | Prominent header button linking to the summaries/generate flow with Sparkles icon. GuildDashboard.tsx:71-76 | v3 header has only title + subtitle (Overview.tsx:42-46); no create-summary affordance. The CreateSummary view exists but is not linked from Overview. Restore: add a button calling |
| medium | guild dashboard home)→Overview | Quick Actions: Configure Channels link | Quick-action card linking to channels config with descriptive copy 'Select which channels to summarize'. GuildDashboard.tsx:157-171 | v3 has no Quick Actions panel at all. ADR-133 A6 says Overview is 'mostly composition of existing data' (read-only) — quick-action navigation shortcuts are not called out as deferr |
| medium | guild dashboard home)→Overview | Quick Actions: Import WhatsApp Chat link | Highlighted quick-action card (green) linking to archive/import with Upload icon and copy 'Upload a WhatsApp export zip file'. GuildDashboard.tsx:189-203 | v3 has a dedicated Whatsapp view but Overview provides no entry point to it. ADR-133 reorganizes WhatsApp under Sources, but the import entry point is not surfaced on the dashboard |
| medium | server info)→/workspaces/summarybot-3/web/src/views/Settings.tsx (per-tenant LLM provider + budget + per-workspace summary instructions) | Timezone selector (grouped Americas/Europe/Asia/Oceania/UTC) with live 'Current time' preview, persisted via TimezoneContext for all dashboard date display. | Grouped Select over TIMEZONE_OPTIONS bound to TimezoneContext; shows current time in chosen tz. v2 Settings.tsx:200-242. | v3 Settings has no global dashboard timezone preference. Timezone exists only as a per-action input (CreateSummary.tsx:99, Schedules.tsx:57, Whatsapp.tsx auto-detect :12-15) defaul |
| medium | server info)→/workspaces/summarybot-3/web/src/views/Settings.tsx (per-tenant LLM provider + budget + per-workspace summary instructions) | Channel Sensitivity Settings (ADR-046): mark channels/categories sensitive so their summaries require admin role to view; includes search, per-channel toggles, category bulk, locked/restricted badges. | <ChannelSensitivitySettings guildId channels categories/> rendered on Settings. v2 Settings.tsx:244-252; component ChannelSensitivitySettings.tsx:1-7,40+ (search/checkbox/switch ov | No channel-sensitivity / per-channel access-gating UI found in v3 Settings or Source.tsx (Source only lists/syncs channels, no sensitivity flag). Not listed in ADR-133's deferrals. |

## Per-page detail

### AdminAudit → Audit  (14 gaps)

_v3 Audit is a minimal, workspace-tenant-scoped read-only list (WSP-014); v2's system-owner-wide audit log with summary KPIs, six filters, pagination, status/severity columns, and a detail dialog is almost entirely absent — only the bare event list + 403 handling survive._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟠unknown | medium | Summary KPI cards (Total Events, Logins, Failed Actions, Security Alerts, Unique Users) | screen | Five summary cards driven by GET /admin/audit/summary showing total_count, login success count, failed_count, alert_count, and unique-user count (AdminAudit.tsx | missing | No summary endpoint or KPI strip in v3 Audit.tsx (only listAudit). Not cited in ADR-133/134. Restore: add /audit/summary aggregation + card grid; or document as |
| 🟠unknown | medium | Event-type text filter (e.g. auth.*) | filter | Free-text Input filtering by event_type wildcard, resets offset on change (AdminAudit.tsx:286-291). | missing | v3 has zero filters; entire list shown unfiltered. Restore by passing event_type to listAudit / backend query param. |
| 🟠unknown | medium | Category filter (Auth/Access/Action/Source/Admin/System) | filter | Select with six audit categories, resets offset (AdminAudit.tsx:300-321, categories list 48-55). | missing | v3 AuditEntry has no category field (types.ts:465-470) and no category filter. Not cited in ADRs. |
| 🟠unknown | medium | Severity filter (Debug/Info/Notice/Warning/Alert) | filter | Select filtering by severity; severities defined with badge colors (AdminAudit.tsx:57-63, 323-344). | missing | v3 AuditEntry has no severity field; no severity filter/column. Not cited in ADRs. |
| 🟠unknown | medium | Pagination (prev/next, showing X-Y of N) | sort_pagination | limit/offset pagination with Previous/Next buttons (disabled at bounds) and a 'Showing N-M of total' counter (AdminAudit.tsx:127-141, 461-486). | missing | v3 fetches a flat first-100 (Audit.tsx:33, listAudit(100)) with no paging and no total count. For high-volume tenants older events are unreachable. Not cited in |
| 🟠unknown | medium | Tabular columns: Timestamp, Event, User, Guild, Category, Status, Severity | field | Seven-column table with resource_type/resource_id sub-line, user icon/name vs System, truncated guild id, category badge, success icon, severity badge (AdminAud | changed | v3 renders a simple list with only action label, timestamp, detail, action code, and optional actor (Audit.tsx:62-78). No resource_type/resource_id, category, s |
| 🟠unknown | medium | Entry detail dialog (click row to expand) | action | Clicking a row opens a Dialog showing event type, full timestamp, user+id, guild, category, status, error_message block, and a JSON details pretty-print (AdminA | missing | v3 rows are non-interactive; no detail view, no error_message or structured details payload. Restore would require backend to expose details/error_message on Au |
| 🟢intentional | low | Guild ID filter | filter | Input to filter audit entries by guild_id (AdminAudit.tsx:293-298). | missing | v3 Audit is scoped to a single workspace's tenant (WSP-014, Audit.tsx:6-9), so a cross-guild filter is structurally moot. Classified intentional per the workspa |
| 🟠unknown | low | Status filter (Success/Failed) | filter | Select filtering by success boolean (AdminAudit.tsx:346-364). | missing | v3 AuditEntry has no success field. Workspace audit events (role changes, invites, identity links) are all completed actions, but failure cases like identity.li |
| 🔵superseded | low | Clear-filters action | action | Ghost button resets all filters to defaults, shown only when hasActiveFilters (AdminAudit.tsx:121-125, 366-371). | missing | Moot in v3 because there are no filters to clear. Tied to the absent filter bar. |
| 🟠unknown | low | Refresh button | action | Outline button calling refetch() to reload logs on demand (AdminAudit.tsx:197-200). | missing | v3 loads once on mount (Audit.tsx:29-47) with no manual refresh affordance. |
| 🟢intentional | low | 403 / access-denied error state | error_state | Dedicated forbidden card with Shield icon, explanatory copy about SYSTEM_OWNER_IDS, and a 'Back to Servers' button; distinguishes 403 from generic errors (Admin | present | v3 handles 403 with an inline message 'You need an admin role on this workspace’s tenant…' (Audit.tsx:40-44). Functionally equivalent for the workspace-scoped m |
| 🟢intentional | low | Loading skeleton | loading_state | Full skeleton matching header + 5 KPI cards + 5 table rows (AdminAudit.tsx:171-173, 580-613). | changed | v3 uses a plain 'Loading…' text (Audit.tsx:57-58). Cosmetic downgrade only; function preserved. |
| 🟢intentional | low | Empty state copy (filter-aware) | empty_state | Empty state varies: 'No events match your current filters' vs 'No audit events have been recorded yet' (AdminAudit.tsx:385-394). | present | v3 has a single helpful empty message explaining events appear as members/roles/invites change (Audit.tsx:80-82). Filter-aware variant is moot since v3 has no f |

### AdminIssues → Issues.tsx  (10 gaps)

_v3 Issues covers the core triage flow and adds status-transition actions v2 lacked, but drops v2's issue-type taxonomy/Type filter, the explicit Status filter, the four summary count cards, the GitHub tracker link, a manual Refresh, and a loading skeleton; some of these are documented as intentional in ADR-133 D4._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟠unknown | medium | Status filter dropdown (All Status / Open / Triaged / On GitHub / Closed) | filter | Select control filtering by status, resets offset on change (AdminIssues.tsx:254-274) | changed | v3 has no explicit status dropdown; it offers only a binary 'Show resolved' checkbox (Issues.tsx:42,97-104,51 includeResolved). Cannot filter to e.g. only 'inve |
| 🟢intentional | low | Issue type taxonomy (bug/feature/question) with icons + labels | field | Each issue has issue_type bug\|feature\|question rendered with a dedicated icon and label (issueTypeConfig at AdminIssues.tsx:79-83; rendered in table at :338-3 | changed | v3 replaces the fixed type taxonomy with a free-text `category` field (types.ts:255 `category: string`; rendered Issues.tsx:143). ADR-133 D4 (line 143) reframes |
| 🔵superseded | low | Type filter dropdown (All Types / Bug / Feature / Question) | filter | Select control filtering issues by issue_type, resets offset to 0 on change (AdminIssues.tsx:233-252) | missing | No type/category filter in v3 (Issues.tsx exposes only Show-resolved checkbox :97-104 and Severity select :105-119). Superseded by the category model change; AD |
| 🔵superseded | low | Summary count cards (Total Issues, Bugs, Features, Questions) | screen | Four metric cards showing total count and per-type counts with colored numerals (AdminIssues.tsx:172-217; counts computed :136-139) | missing | v3 shows only an 'N open' badge in the header (Issues.tsx:86-90) plus data.total/categories/severities available in payload (types.ts:269-273) but not surfaced  |
| 🟢intentional | low | Per-issue GitHub tracker link (row 'View' link + dialog 'View on GitHub' button) | action | github_issue_url rendered as external link in the GitHub table column (AdminIssues.tsx:349-362) and as a full-width button in the detail dialog (:469-478) | missing | ADR-133 D4 (line 143) explicitly defers GitHub-tracker linkage: 'GitHub-tracker linkage remains deferred (we keep triage in-app; not wiring a public GitHub mirr |
| 🟠unknown | low | Manual Refresh button | action | Outline 'Refresh' button calling refetch() (AdminIssues.tsx:166-169) | missing | v3 auto-loads on filter/page change via useEffect (Issues.tsx:62-64) and reloads after a status change (:74), but has no user-triggered refresh affordance. Low  |
| 🟠unknown | low | Loading skeleton state | loading_state | Dedicated AdminIssuesSkeleton with header, four card, and five row skeletons shown while isLoading (AdminIssues.tsx:142-144,487-520) | missing | v3 has no loading state; data starts null and the list/empty-state render directly (Issues.tsx:41,124). Initial render shows neither rows nor an explicit empty/ |
| 🟢intentional | low | Error state handling | error_state | No explicit error UI; useIssues relies on react-query without an error branch (AdminIssues.tsx:92-105,112) | changed | v3 catches fetch failures and falls back to EMPTY data (Issues.tsx:57-59), which renders the 'No reports' empty state. This is arguably a slight improvement ove |
| 🟢intentional | low | Issue detail dialog (full description, ID, formatted timestamps, browser, guild, page URL) | screen | Clicking a row opens a Dialog showing title, copyable ID, PPpp-formatted created date, full description, status badge, page URL link, browser_info, guild_id, an | changed | v3 surfaces description (Issues.tsx:150), status/severity badges (:133-142), category (:143), page_url (:144-148), reporter + created_at (:151-153), and truncat |
| 🟢intentional | low | Status-transition action buttons (Investigating/Resolve/Won't fix/Reopen) | action | v2 is read-only triage: rows open a detail dialog but offer no status-change controls (AdminIssues.tsx has no mutation, only useQuery) | present | v3 ADDS per-issue status transitions via setIssueStatus (Issues.tsx:71-75, NEXT map :21-33, buttons :163-171) — a capability v2 lacked. Recorded as a v3 improve |

### Archive → CreateSummary (Past flow) + Coverage + Jobs + Whatsapp + Spend (Archive is split across these; no single 1:1 view)  (15 gaps)

_v3 deliberately split v2's monolithic Retrospective page (ADR-133 §A8 "Populate covered by Create→Past/retrospective"). Core scan/gaps, jobs, costs, and import are all present in some form, but the rich Generate dialog loses cost estimation, summary-type/perspective selection, skip/force-regenerate toggles, Confluence auto-publish, and the per-source pre-generation gap scan; the v2 Discord/WhatsApp generate path is largely WhatsApp-only in v3._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔴regression | high | Pre-generation cost estimate (Estimate button → periods/tokens/cost/model) | action | GenerateDialog has an Estimate button calling onEstimate(buildRequest(true)) (dry_run) and renders periods, estimated_tokens, estimated_cost_usd, model before c | missing | CreateSummary's Past flow (CreateSummary.tsx:106-110) calls summarizeWeeks with no dry-run/estimate step. No v3 view exposes a pre-flight cost estimate for retr |
| 🔴regression | medium | Summary Type selector (Brief/Detailed/Comprehensive → model tier) | field | GenerateDialog summaryType select brief(Haiku)/detailed(Sonnet)/comprehensive(Sonnet), threaded into summary_type. Archive.tsx:727, 901-913 | missing | CreateSummary's Past flow exposes no summary length/type control (CreateSummary.tsx:299-303 just runs summarizeWeeks). ADR-133 §B notes Summaries/Schedules gain |
| 🔴regression | medium | Perspective selector (General/Developer/Executive/Marketing/Product/Finance/Support) | field | GenerateDialog perspective select with 7 perspectives, threaded into the generate request. Archive.tsx:728, 914-930 | missing | No perspective picker in CreateSummary's Past flow. ADR-133 §A2 ships Prompts/Perspectives as a tab and §B adds Perspective to Schedules/Summaries, but the retr |
| 🔴regression | medium | Skip-existing / Regenerate-failed / Force-regenerate toggles | default | Three switches: skip_existing (default on), regenerate_failed (default on), force_regenerate (default off, 'Delete existing and regenerate all'). Archive.tsx:72 | missing | CreateSummary Past flow has no idempotency controls; summarizeWeeks(chat) handles empty-week skipping server-side but exposes no skip/force/regenerate-failed UI |
| 🔴regression | medium | Max Cost (USD) cap input | field | Optional max_cost_usd numeric input that bounds the generation spend (request.max_cost_usd = parseFloat). Archive.tsx:732, 774-776, 982-991 | missing | No spend cap on any v3 generation flow (CreateSummary Past has no cost field). Not addressed in ADR-133. Users cannot bound retrospective cost up front. |
| 🔵superseded | low | Per-source gap scan (Complete/Failed/Missing/Outdated stats + gap list) | screen | SourceCard expands to a scan view: useScanSource, StatBoxes for complete/failed/missing/outdated, and a gaps list (start→end, reason badge, days) capped at 10 w | changed | Coverage.tsx generalizes this to all sources (ADR-133 §A1): per-channel coverage %, message/summary/gap counts, content range. It drops the complete/failed/miss |
| 🔵superseded | low | Auto-publish to Confluence toggle on generate | action | GenerateDialog auto_publish_confluence switch (disabled unless Confluence configured) that publishes each summary as generated; progress banner shows confluence | missing | v3 moved publishing to per-summary/bulk Publish actions on the Summaries view (ADR-133 D2: POST /summaries/:id/publish + bulk). Auto-publish-on-generate is not  |
| 🔵superseded | low | Discord/Slack scope selection for retrospective generation (ScopeSelector: channel/category/guild) | filter | GenerateDialog renders ScopeSelector for Discord (channel_ids / category_id / guild scope); sourceType select offers Discord/WhatsApp/Slack. Archive.tsx:724, 80 | changed | CreateSummary step 1 supports platform (WhatsApp/Discord/Slack), server, channel, and category scope (CreateSummary.tsx:147-213), but the Past/retrospective bra |
| 🟠unknown | low | Bulk delete empty sources + per-source delete | action | handleBulkDeleteEmpty deletes all 0-summary sources with confirm + toast count; SourceCard shows a Trash2 delete button for empty sources. Archive.tsx:147-175,  | missing | Neither Coverage (read-only, Coverage.tsx:5-7) nor Whatsapp exposes source deletion or bulk-delete of empty sources. Not mentioned in ADR-133. WhatsApp dedups o |
| 🔵superseded | low | Hide-empty-sources toggle + source search + source-type filter | filter | Sources tab: search box (name/channel), source-type Select (all/discord/whatsapp/slack), and a 'Hide empty sources (N hidden)' Switch defaulting on. Archive.tsx | changed | Coverage lists channels with no search/type filter/hide-empty toggle (Coverage.tsx:96-113); it does show gap/coverage per channel. Source-type filtering exists  |
| 🟢intentional | low | Job pause / resume controls | action | JobsView renders Pause (running) and Resume (paused) buttons calling pauseJob/resumeJob, plus Cancel. Archive.tsx:1299-1336, 1599-1649 | changed | ADR-133 §D1: pause/resume are 'N/A by architecture' (jobs run synchronously, nothing to suspend). v3 Jobs.tsx offers Retry for scheduled jobs only (Jobs.tsx:245 |
| 🟢intentional | low | Job cancel control | action | JobsView Cancel button on running/paused/pending jobs via cancelJob; active-job progress banner has a Cancel button. Archive.tsx:306-313, 1338-1352, 1609-1648 | missing | Follows from ADR-133 §D1 synchronous-jobs rationale — no running process to cancel; Jobs.tsx exposes only Retry. Consistent with pause/resume being N/A. |
| 🔵superseded | low | Per-source cost report (Costs tab: total cost/summaries + by-source with this-month) | screen | CostsView shows total_cost_usd, total_summaries, and a By-Source list with total_cost_usd + current_month.cost_usd per source. Archive.tsx:1174-1245, 494-495 | changed | ADR-133 §C: v3 has a dedicated Spend analytics dashboard that exceeds v2's per-job cost. Cost reporting intent satisfied by a separate, richer Spend view rather |
| 🔵superseded | low | Active-job live progress banner + completion banner with 'View Summaries' | loading_state | Inline running-job banner (Loader, X of Y complete, failed count, Progress bar, Cancel) and a green completion banner with summary count + 'View Summaries' nav. | changed | CreateSummary shows a synchronous result string + inline produced-summary preview (CreateSummary.tsx:318-330) rather than an async job-progress banner; Jobs.tsx |
| 🟠unknown | low | WhatsApp import advanced options (Reader Bot JSON format, group-name/id override) + filename-derived name preview | field | ImportDialog advanced section: format select whatsapp_txt/reader_bot, Group Name + Group ID override inputs, and a filename-derived name preview card. Archive.t | changed | Whatsapp.tsx import accepts .zip/.txt with optional channel-id + timezone and auto-detects group name + date format (Whatsapp.tsx:97-123), but offers no Reader  |

### AuditLog → Audit (web/src/views/Audit.tsx)  (15 gaps)

_v3 Audit is a deliberately narrow security/admin event log (WSP-014: roles, invites, identity links) and is markedly thinner than v2's general-purpose audit log — it drops summary stat cards, all filtering, severity/category/success classification, resource/error/details, the per-entry detail dialog, pagination, and refresh. ADR-133 maps v2 Audit to the v3 Audit tab but does NOT list it for enrichment, so most deltas are unclassified parity gaps rather than documented decisions._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟠unknown | medium | Summary stat cards (Total Events / Failed Actions / Security Alerts / Unique Users) | screen | Four summary cards driven by useAuditSummary: total_count, failed_count, alert_count, unique users from Object.keys(summary.by_user) (AuditLog.tsx:135-188); plu | missing | No equivalent summary/aggregate in v3 Audit.tsx (it only lists entries). Restore: add an audit-summary endpoint + 4 stat cards. Not addressed in ADR-133 §B (Aud |
| 🟠unknown | medium | User-name text filter | filter | Input bound to filters.user_name, resets offset, narrows entries by actor name (AuditLog.tsx:205-210). | missing | v3 has no filters at all (Audit.tsx:49-87). Restore as a client/server filter on actor. |
| 🟠unknown | medium | Event-type filter (e.g. auth.*) | filter | Input bound to filters.event_type with wildcard hint placeholder, resets offset (AuditLog.tsx:212-217). | missing | v3 has no event-type filter. v3 shows the raw action code as a <code> chip (Audit.tsx:73) but cannot filter on it. |
| 🟠unknown | medium | Category filter + category taxonomy | filter | Select over 6 categories (auth/access/action/source/admin/system) with 'All Categories' default; resets offset (AuditLog.tsx:46-53,219-240). Each entry shows a  | missing | v3 AuditEntry has no category field (types.ts:465-470) and no category filter/badge. v3 events are an implicitly narrow set (member/invite/identity) so categori |
| 🟠unknown | medium | Severity filter + severity badges | filter | Select over 5 severities (debug/info/notice/warning/alert) with color-coded badges and 'All Severity' default (AuditLog.tsx:55-61,242-263); per-entry severity B | missing | v3 AuditEntry has no severity (types.ts:465-470); no severity filter or badge. Distinct from ADR-133 A3 Errors (which carries severity for operational errors, e |
| 🟠unknown | medium | Success/failure status filter and indicators | filter | Select All/Success/Failed bound to filters.success (AuditLog.tsx:265-283); per-row CheckCircle (green) / AlertTriangle (red) status icon (AuditLog.tsx:366-372). | missing | v3 AuditEntry has no success field; no status filter or per-row indicator. v3 events appear to be successful-action records only, but rejections like identity.l |
| 🟠unknown | medium | Pagination (offset/limit, Showing X-Y of N, Prev/Next) | sort_pagination | Server-side paging via filters.limit/offset with handlePrevPage/handleNextPage, disabled-state buttons, and 'Showing N - M of total' counter (AuditLog.tsx:89-10 | missing | v3 fetches a single fixed page of 100 (api.ts:569 listAudit(100); Audit.tsx:33) with no total, offset, or next/prev. Entries beyond 100 are silently unreachable |
| 🟠unknown | medium | Per-entry detail dialog | action | Clicking a row opens a Dialog showing event type, full timestamp (PPpp), user + id, category, status, severity, resource (type/id/name), error_message block, pr | missing | v3 rows are static <li>s with no click target or detail view (Audit.tsx:63-77). v3 AuditEntry lacks the underlying fields (resource, error_message, details, dur |
| 🔵superseded | low | Clear-filters control | action | Ghost 'Clear' button shown when hasActiveFilters, resets filters to {limit:50,offset:0} (AuditLog.tsx:83-87,285-290). | missing | Superseded only because v3 has no filters to clear. Tied to the missing-filters gaps above. |
| 🟠unknown | low | Refresh button | action | Outline 'Refresh' button calling refetch() (AuditLog.tsx:128-131). | missing | v3 loads once on mount (Audit.tsx:29-47) with no manual refresh affordance. |
| 🟠unknown | low | Resource / error_message / details / duration fields | field | Entries carry resource_type/id/name, error_message, arbitrary details JSON, and duration_ms, surfaced in the row subtext and detail dialog (AuditLog.tsx:339-344 | missing | v3 AuditEntry is {ts,actor,action,detail} only (types.ts:465-470); detail is a single human string (Audit.tsx:71). No structured resource/error/duration data. |
| 🟢intentional | low | Loading state | loading_state | Full skeleton layout (header + 4 stat cards + filter bar + 5 row skeletons) via AuditLogSkeleton (AuditLog.tsx:105-107,513-555). | changed | v3 shows a plain 'Loading…' text line (Audit.tsx:57-58). Functionally equivalent; cosmetic-only downgrade, acceptable. |
| 🟢intentional | low | Empty state | empty_state | Shield icon + heading + filter-aware message ('No events match your current filters.' vs 'No audit events have been recorded yet.') (AuditLog.tsx:304-313). | changed | v3 has a single empty message tailored to its scope: 'No audit events yet — they appear here as members, roles, and invites change.' (Audit.tsx:79-83). No filte |
| 🔵superseded | low | 403 / error state | error_state | v2 useAuditLogs has no explicit in-component error branch (only isLoading); errors are not surfaced on this page (AuditLog.tsx:80,105-107). | present | v3 IMPROVES on v2: explicit admin-only 403 copy ('You need an admin role on this workspace’s tenant to view the audit log.') and a generic load-failure message  |
| 🔵superseded | low | Action label mapping | copy | v2 maps category->label and severity->label, but shows raw event_type strings for events (no per-action friendly labels) (AuditLog.tsx:46-73,338). | present | v3 adds friendly ACTION_LABELS for its known actions (e.g. member.role_set -> 'Role changed') while still showing the raw code chip (Audit.tsx:10-17,67,73). Equ |

### Channels → Source (web/src/views/Source.tsx)  (14 gaps)

_v3 Source is the counterpart but reframes Channels from a persistent enable/disable summarization-scope config (with locked-channel governance and category bulk toggles) into a transient live-sync channel picker; the live-sync reframing and split-out WhatsApp/Slack tabs are largely intentional (ADR-128/133), but the restricted/locked-channel governance flow and aggregate warning banners appear unimplemented rather than explicitly deferred._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔵superseded | high | Persistent per-channel enable/disable config (enabled_channels saved to guild config) | behavior | Channels are toggled on/off via Switch and the selection is persisted to guild.config.enabled_channels via updateConfig.mutateAsync (Channels.tsx:50,58,82-93,11 | missing | v3 Source.tsx treats channel selection as transient: `selected`/`channels` only choose what to fetch in the current sync (Source.tsx:62,182-190,119-124) and is  |
| 🟠unknown | high | Locked/restricted channel governance (🔒 badges, confirm dialog, audit warnings) | behavior | Channels with is_locked are disabled-by-default, visually flagged with Lock icon + 'Restricted', enabling one triggers a window.confirm warning that the action  | missing | No is_locked/locked_override concept in v3 web (grep: zero hits). v3 Source only distinguishes accessible vs inaccessible (Source.tsx:342-357). ADR-133 lists Ch |
| 🔵superseded | medium | Save Changes / Saved button with dirty-state tracking | action | A Save button shows 'Save Changes' when hasChanges is true and 'Saved' otherwise, disabled when no changes, with a success toast on save (Channels.tsx:51,186-19 | missing | No save/dirty-state on v3 Source — selection feeds the immediate Sync action instead. Consequence of the live-sync model change above. |
| 🔵superseded | medium | Category-level Enable All / Disable All bulk toggle | action | Each category card has 'Enable All' and 'Disable All' buttons that toggle every channel in the category, disabled appropriately when all/none enabled (Channels. | missing | v3 groups channels by category (Source.tsx:330-362,465-480) but offers no per-category bulk select; only individual checkboxes. Restore hint: add per-group sele |
| 🟢intentional | medium | Three top-level warning alerts (inaccessible / locked-summarizing / restricted-detected) | error_state | Page renders up to three banner Alerts: red 'N channels not accessible … grant Read Message History', amber 'N restricted channels being summarized … may contai | changed | v3 conveys inaccessibility only inline (per-channel '🔒 no access' label + disabled checkbox, Source.tsx:342-357) and post-sync per-channel failure list (Source. |
| 🟢intentional | medium | Slack workspace channel listing with Primary + Auto-summarize badges | screen | For each linked Slack workspace, a card lists up to 20 channels with Hash icon, a 'Primary' badge for the primary workspace, channel-count badge, an 'Auto-summa | changed | v3 Source handles Slack via the same generic platform browser (token + channel directory, Source.tsx:16-42,49) selected by the platform prop, not a per-workspac |
| 🟢intentional | low | Per-category enabled count header (e.g. 3/8) and collapsible categories | behavior | Each category header shows enabledCount/total and is a collapsible section with open/close state (Channels.tsx:248,283-285,262-275,279). | changed | v3 shows categories as static grouped lists (always expanded) with a single overall '{n} selected' line (Source.tsx:363-365), no per-category counts or collapse |
| 🟢intentional | low | Inaccessible-channel count badge in category header | behavior | Each category header shows a destructive badge 'N no access' counting channels with bot_can_read===false in that category (Channels.tsx:252,286-292). | missing | v3 flags inaccessible channels inline per-row (Source.tsx:353-357) but has no per-category aggregate count. Equivalent information available inline; ADR-097 acc |
| 🟢intentional | low | Sync Channels (refresh channel list) action with spinner + toast | action | A 'Sync Channels' button refreshes the channel list via syncChannels.mutateAsync, spins the RefreshCw icon while pending, and toasts success/error (Channels.tsx | present | v3 has an equivalent 'Load channels'/'Refresh' button on the Browse channels panel with loading state (Source.tsx:312-319). Semantics differ (v2 refreshes Disco |
| 🟢intentional | low | WhatsApp imports listed on the Channels surface | screen | Channels page lists WhatsApp import sources for the guild (filtered from archiveSources by 'whatsapp:' prefix) with name, summary-count badge, and 'Imported' ba | present | v3 has a dedicated Whatsapp.tsx view rather than embedding WhatsApp on the channel surface. ADR-133 navigation maps SOURCES → 'All Sources · Channels · WhatsApp |
| 🟢intentional | low | 'View all sources →' navigation link | navigation | When Slack/WhatsApp sources exist, a centered 'View all sources →' link routes to /guilds/:id/sources (Channels.tsx:456-466). | changed | v3 navigation already exposes 'All Sources', 'Channels'(Source), and 'WhatsApp' as sibling nav tabs (ADR-133 line 40), making an in-page cross-link unnecessary. |
| 🟢intentional | low | Per-channel type icon (text Hash / voice Volume2 / forum MessageSquare) | field | Each channel row shows a type-specific icon based on channel.type (Channels.tsx:23-27,318,337). | missing | v3 SourceChannel has no type field (types.ts:514-522) and renders every channel with a uniform '#' prefix (Source.tsx:350-352). v3 directory is text-channel ori |
| 🟢intentional | low | Loading skeleton for the page | loading_state | While the guild loads, a full ChannelsSkeleton renders header + three skeleton category cards (Channels.tsx:154-156,550-578). | changed | v3 Source has no page-level skeleton; it uses per-action button loading text ('Loading…','Saving…','Syncing…', Source.tsx:276,318,392) and starts with empty pan |
| 🟢intentional | low | Lookback window control | field | (no equivalent in v2 Channels — selection only) | present | v3 adds a 'Look back N day(s)' numeric input feeding the sync window (Source.tsx:376-386). This is a v3 addition tied to its live-sync model, not a v2 gap; note |

### Coverage → Coverage (web/src/views/Coverage.tsx)  (9 gaps)

_v3 Coverage is an intentionally read-only subset (ADR-133 A1): it keeps the four overview stats, content range, and a per-channel list, but drops the entire backfill subsystem, the gaps tab with status filtering/per-gap detail, the platform selector, full-refresh recompute, channel-name display, and channel→summaries navigation. Most omissions are documented v3 decisions; a few are unrecorded regressions._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔴regression | medium | Channel display name (falls back to id) | field | Channel rows and gap rows show channel_name \|\| channel_id, so users see human-readable '#job-board' names (Coverage.tsx:233, 302; ChannelCoverage.channel_name | changed | v3 ChannelCoverage (types.ts:178-188) has no channel_name; view renders raw channel_id in mono font (Coverage.tsx v3:100). ADR-133 live-evidence (line 61) expli |
| 🟢intentional | medium | Scheduled Backfill controls (start/pause/resume/cancel, priority mode, rate limit, progress, failed count) | action | Full BackfillControls sidebar: start with priority_mode (oldest_first/newest_first/largest_gaps) + rate_limit slider (1-50/hr), live progress %, completed/total | missing | ADR-133 A8 (line 96): 'Populate'/backfill is 'covered by Create→Past / retrospective / Sync', not built into Coverage which is read-only over existing data. Res |
| 🟢intentional | medium | Coverage Gaps tab with per-gap rows (status, priority badge, date range, gap_days, error_message) and a status filter (all/pending/scheduled/running/complete/failed) | filter | Tabbed Coverage Gaps list backed by GET /coverage/gaps?status=&limit=100; each gap shows channel, date range, days, priority badge, status icon/label, and error | missing | v3 WorkspaceCoverageGap (types.ts:170-175) carries only start/end/kind, embedded per-channel, with no status/priority/error — gaps are descriptive not actionabl |
| 🟠unknown | medium | Per-channel navigation to filtered summaries | navigation | Each channel row is a Link to /guilds/:id/summaries?channel=<id> with a ChevronRight affordance, letting users drill from a channel's coverage into its summarie | missing | v3 channel rows are static <li> with no link (Coverage.tsx v3:97-112). Not mentioned in ADR-133's read-only framing as removed; likely an unrecorded affordance  |
| 🟢intentional | low | Platform selector (Discord / WhatsApp / Slack) | filter | Header Select switches coverage platform; drives platform query param on coverage, gaps, and refresh; report.platform shown as a badge (Coverage.tsx:106, 529, 5 | missing | ADR-133 A1 (line 89) generalizes WhatsApp-only coverage to ALL sources in a single unified report rather than a per-platform toggle; WorkspaceCoverage has no pl |
| 🟢intentional | low | 'Full Refresh' recompute action (include_inventory) | action | Two header buttons: 'Refresh' (include_inventory=false) and 'Full Refresh' (include_inventory=true) POST to /coverage/refresh to recompute coverage from summari | missing | ADR-133 D5 (line 144) explicitly lists Coverage 'Full refresh' recompute as low-value, 'do last or skip' (❌). v3 'Refresh' button just re-fetches the cached rep |
| 🟠unknown | low | Per-channel coverage progress bar + covered/total days + per-channel color tone | field | Each channel row renders a colored mini progress bar (green/yellow/red by %), 'X summaries', 'covered/total days', and 'N gaps' in orange; list sorted ascending | changed | v3 shows percent (color-toned via pctTone), message_count, summary_count, gap_count, and content range, but no progress bar, no covered/total days, and no sort  |
| 🔵superseded | low | Distinct skeleton loading states for cards and lists | loading_state | Overview cards and both channel/gaps lists render Skeleton placeholders while their queries load (Coverage.tsx:618-624, 684-689, 721-726) | changed | v3 uses a single Refresh button label 'Loading…' and renders nothing until cov resolves (Coverage.tsx v3:62-67, 72). Functionally adequate for a single-fetch re |
| 🟢intentional | low | 'No coverage data' guidance prompting Full Refresh | empty_state | When no report exists, an Alert instructs the user to click 'Full Refresh' to compute coverage from summaries and Discord history; channel/gaps lists have their | changed | v3 empty state for channels says 'No channels with stored messages yet — sync a source or import a chat' (Coverage.tsx v3:91-95), redirecting to the v3 ingestio |

### Errors → Errors (web/src/views/Errors.tsx)  (15 gaps)

_v3 ships the core operational error log (list, severity/class/operation/scope, single + bulk resolve, show-resolved toggle, empty/loading-ish states) but drops the entire filtering layer (multi-select error-type filter, severity filter, Missing-Access toggle), the per-error detail drawer (stack trace, additional details, error code, resolution notes, Report-Issue), the type-scoped bulk-resolve menu with confirmation, and pagination count — mostly consistent with ADR-133 A3 "read + resolve only" but several are uncalled-out reductions._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟢intentional | medium | Error detail drawer/dialog (per-error click-through) | action | Clicking any error opens ErrorDetailDrawer (desktop Dialog / mobile Drawer) with full detail view; ErrorCard onClick wired at Errors.tsx:367-373 and drawer at E | missing | v3 renders a flat inline list with no click-through (Errors.tsx:101-137). ADR-133 A3 scopes Errors to a read+resolve log (docs/adr/ADR-133-v2-dashboard-parity-m |
| 🟢intentional | medium | Stack trace in error detail | field | Detail drawer shows a scrollable monospace stack_trace block when present (ErrorDetailDrawer.tsx:185-197). | missing | v3 OperationalError type has no stack_trace field (web/src/types.ts:226-236); not surfaced anywhere. Consistent with ADR-133 A3 minimal log scope but not indivi |
| 🟢intentional | medium | Resolution notes (capture + display) | field | Resolve flow offers an optional notes Textarea (ErrorDetailDrawer.tsx:249-258); handleResolve passes notes (Errors.tsx:89-104); resolved errors display resoluti | missing | v3 resolveError takes only an id (web/src/views/Errors.tsx:38-42) and OperationalError has no resolved_at/resolution_notes (web/src/types.ts:226-236). ADR-133 A |
| 🟢intentional | medium | Report Issue from an error | action | Detail drawer 'Report Issue' button opens ReportIssueDialog pre-seeded with full error context (id, type, code, message, operation, severity, stack, details) an | changed | v3 has a standalone Issues view (ADR-039) but no error->report bridge with prefilled context. ADR-133 D4 covers Report-Issue fidelity generally (docs/adr/ADR-13 |
| 🟢intentional | medium | Multi-select Error Type filter + selected-type badges | filter | Dropdown multi-select over 9 error types with count badge and removable selected-type chips; toggleErrorType + filters.error_types (Errors.tsx:40-50, 130-141, 2 | missing | v3 has no error-type taxonomy (OperationalError carries only error_class string, web/src/types.ts:229) and no type filter at all (web/src/views/Errors.tsx). ADR |
| 🟢intentional | medium | Severity filter (dropdown) | filter | Select to filter by critical/error/warning/info, default 'All Severity' (Errors.tsx:52-57, 282-302). | missing | v3 has no severity filter; also v3 severity is only 'error'\|'warning' (web/src/types.ts:230-231, SEV_TONE web/src/views/Errors.tsx:8-11) vs v2's four levels (c |
| 🟢intentional | medium | Show Missing Access toggle (hide discord_permission by default) | filter | discord_permission errors are hidden by default; a Switch reveals them and they are filtered client-side, also affecting unresolved_count (Errors.tsx:68-85, 304 | missing | v3 has no error-type concept so cannot single out Missing Access, and no such toggle (web/src/views/Errors.tsx:85-92 only has Show resolved). v2 hid permission  |
| 🟢intentional | medium | Type-scoped bulk resolve menu + confirmation dialog | action | 'Bulk Resolve' dropdown lists each unresolved type with its count and resolves only that type, behind an AlertDialog confirmation ('cannot be undone') (Errors.t | changed | v3 'Bulk resolve' is a single button that resolves ALL unresolved errors via resolveAllErrors with no per-type scoping and no confirmation (web/src/views/Errors |
| 🟢intentional | low | Additional details (key/value metadata) | field | Detail drawer renders error.details as a definition list when non-empty (ErrorDetailDrawer.tsx:170-184). | missing | No details field in v3 OperationalError (web/src/types.ts:226-236). Within ADR-133 A3 reduced scope; backend would need a details payload to restore. |
| 🟢intentional | low | Error code field | field | Detail drawer shows error.error_code ('Code: …') when present (ErrorDetailDrawer.tsx:122-126). | missing | v3 has error_class (web/src/types.ts:229) but no error_code; ADR-133 A3 lists 'class' not 'code' as the shipped field (docs/adr/ADR-133-...:91). Effectively sup |
| 🟢intentional | low | Pagination / 'Showing N of M' count + limit | sort_pagination | Fetches with limit:100 default and shows 'Showing {n} of {total} errors' when total exceeds the limit (Errors.tsx:63-67, 375-379). | missing | v3 listErrors takes only the include-resolved boolean (web/src/views/Errors.tsx:19-32) with no limit/total surfaced and no count line. Within ADR-133 A3 scope;  |
| 🟢intentional | low | Channel name display | field | ErrorCard and detail drawer show a human channel_name with a Hash icon (ErrorCard.tsx:96-104; ErrorDetailDrawer.tsx:142-150). | changed | v3 shows the raw channel_id (#<id>, monospace) not a resolved name (web/src/views/Errors.tsx:115-117; type has channel_id only, web/src/types.ts:232). ADR-133 f |
| 🟢intentional | low | Loading skeleton state | loading_state | Dedicated ErrorsSkeleton with header/filter/list placeholders while isLoading (Errors.tsx:161-163, 418-444). | changed | v3 has no skeleton; it shows 'Loading…' text on the Refresh button and renders nothing in the list area while busy (web/src/views/Errors.tsx:16, 77-82). Functio |
| 🟢intentional | low | Filter-aware empty state copy + Clear filters | empty_state | Empty state varies by context (active filters vs include_resolved vs default) and a 'Clear' button resets all filters (Errors.tsx:328-333, 340-359). | changed | v3 empty state is a single line ('No errors… operations are healthy') with a minor show-resolved variant and no Clear control (web/src/views/Errors.tsx:96-100). |
| 🟢intentional | low | Toast feedback on resolve / bulk resolve | behavior | Success and failure toasts on single resolve and bulk resolve, with resolved-count in the message (Errors.tsx:89-123). | changed | v3 resolve/resolveAll silently reload with no success/error toast and no resolved-count feedback (web/src/views/Errors.tsx:38-48). Errors are swallowed in load( |

### Feeds → Feeds (/workspaces/summarybot-3/web/src/views/Feeds.tsx)  (11 gaps)

_v3 Feeds covers the core CRUD-lite loop (list/create/delete/copy/open) but drops the entire ADR-037 filter-criteria subsystem, the edit flow, token regeneration, the channel dropdown, and most form fields (description, feed_type/Atom, max_items, include_full_content), plus loading/error/confirm states._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔴regression | high | Edit feed flow (Edit dialog + per-card Edit action) | action | v2 has a full Edit dialog reusing FeedForm and a per-card pencil Edit button; openEditDialog pre-fills form from the feed including criteria (Feeds.tsx:87-100,  | missing | No edit affordance in v3 — feeds can only be created and deleted. Restore an edit dialog (title/description/is_public/max_items/criteria) backed by an updateFee |
| 🔴regression | high | Filter criteria subsystem (ADR-037) on create/edit | filter | v2 FeedForm exposes a collapsible Filter Criteria section (FilterCriteriaForm + FilterCriteriaSummary, active-count badge) covering source/channels/platform/per | missing | Entire feed-content filtering capability absent in v3; only a single optional channel_id is supported. ADR-133 A4 spec (line 92) does not list criteria, so this |
| 🔴regression | high | Regenerate token action (private feeds) | action | v2 offers a per-card RefreshCw Regenerate Token action (private feeds only) guarded by an AlertDialog warning that old URLs stop working; copies new URL to clip | missing | ADR-133 explicitly lists `Regenerate` as a shipped affordance for feeds (line 11). No regenerate action in v3 Feeds.tsx — users cannot rotate a leaked token. Ad |
| 🔴regression | medium | Channel selector (dropdown of guild channels) | field | v2 FeedForm renders a Select populated with the guild's text channels plus 'All Channels', with helper copy (FeedForm.tsx:96-120). | changed | v3 replaces the dropdown with a free-text 'Channel id' input (Feeds.tsx:71-76), forcing users to know raw channel IDs and showing the raw id (`#{channel_id}`) r |
| 🔴regression | medium | Feed Type selector (RSS vs Atom) | field | v2 FeedForm lets the user choose RSS 2.0 or Atom 1.0 at create time (FeedForm.tsx:123-142); cards show the chosen type badge. | missing | v3 has no feed_type control (Feeds.tsx:30-45 omits it); feeds are implicitly RSS. ADR-133 A4 (line 92) and the view header still name 'RSS/Atom', so Atom output |
| 🔴regression | medium | Description, Max Items, Include Full Content fields | field | v2 FeedForm has a Description textarea (FeedForm.tsx:155-165), a Max Items slider 1-100 with live value + helper (FeedForm.tsx:234-250), and an Include Full Con | missing | v3 create form only has title/channel/public (Feeds.tsx:64-88). These three feed-shaping fields are absent; ADR-133 A4 schema (line 92) does not mention them, s |
| 🔴regression | medium | Delete confirmation dialog | validation | v2 wraps the delete action in an AlertDialog with a destructive-consequences warning ('Feed readers using this URL will no longer receive updates') before delet | changed | v3 deletes immediately on click with no confirmation (Feeds.tsx:47-51, 132-137). Add a confirm step for this irreversible, externally-visible action. |
| 🔴regression | low | Loading skeleton state | loading_state | v2 renders FeedsSkeleton (three card placeholders) while feeds load (Feeds.tsx:299-300, 349-377). | missing | v3 starts with feeds=null and shows nothing until loaded (Feeds.tsx:10, 92-93 only handle length===0). Add a loading indicator. |
| 🔴regression | low | List/error handling on load failure | error_state | v2 surfaces failures via toasts on create/edit/delete/regenerate (Feeds.tsx:124-130 etc.) and distinguishes empty vs loading. | changed | v3 swallows list-load errors by setting feeds=[] (Feeds.tsx:21-23), so a failed fetch is indistinguishable from 'No feeds yet'; create has no try/catch user fee |
| 🔴regression | low | Per-feed stats (last accessed, created date) | field | v2 FeedCard shows access count, 'Last accessed <relative>' and 'Created <relative>' (FeedCard.tsx:112-125). | changed | v3 shows only access_count (Feeds.tsx:109-111); last_accessed and created_at are not displayed. |
| 🟢intentional | low | Feed Preview sheet (per-card Eye action) | screen | v2 has a FeedPreviewSheet showing rendered feed items with pagination/Load More, active filters, item badges, and click-through to summaries; opened via a per-c | missing | ADR-133 D5 (line 144) explicitly defers the Feed Preview sheet as 'low value; do last or skip.' v3 instead offers an 'Open' link to the raw feed URL. |

### Guilds → App.tsx workspace switcher + Overview.tsx + Prompts.tsx (no single Guilds equivalent)  (9 gaps)

_v2 /guilds is a multi-server picker landing + built-in default-prompt viewer; v3 collapses server-picking into a minimal workspace <select> (per ADR-133 single-workspace context) and delivers template CRUD via Prompts, but drops per-server stats/status, the Refresh-Servers action, and the read-only built-in prompt content viewer._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟢intentional | medium | Server/workspace card grid (rich picker landing page) | screen | v2 renders all of a user's servers as a responsive card grid, each card showing icon/avatar, name, status badge, member count, summary count, and last-summary d | changed | v3 replaces the multi-server picker with a plain <select> workspace switcher in the app shell (App.tsx:120-138), shown only when >1 workspace. ADR-133 (Context, |
| 🟠unknown | medium | Refresh Servers button (re-fetch server list from Discord) | action | A 'Refresh Servers' button calls POST /auth/refresh, updates the token, re-fetches guilds, with spinner + success toast and a SESSION_EXPIRED-aware error toast  | missing | No way in v3 to re-pull the granted workspace set from the identity provider; client.workspaces() is fixed from the session token (App.tsx:120-135). The user mu |
| 🟢intentional | medium | Built-in default-prompt content viewer (by perspective/length and by category) | screen | DefaultPromptsCard shows built-in prompts in two tabs — 'By Perspective' (general/developer/marketing/executive/support, each with description and brief/detaile | changed | v3 Prompts.tsx (ADR-133 A2, line 90) renders built-in perspectives only as non-interactive label pills (Prompts.tsx:90-99) — no length variants, no descriptions |
| 🟢intentional | low | Per-server config status badge (configured / needs_setup / inactive) | field | Each guild card shows a status badge driven by guild.config_status with three states and icons (Guilds.tsx:30-36,54-57; type at types/index.ts:23). | missing | No equivalent per-workspace config-status indicator on the v3 switcher. ADR-133 A6 (line 94) folds 'config status' into the Overview dashboard rather than the p |
| 🟢intentional | low | Per-server stats (member_count, summary_count, last_summary_at, icon) | field | Guild card shows member_count.toLocaleString(), summary_count, last_summary_at, and the server icon/avatar (Guilds.tsx:48-79; types/index.ts:19-22). | changed | v3 surfaces summary_count/member_count/etc. on the Overview dashboard (Overview.tsx:51-54) rather than on a per-server picker card. Acceptable per ADR-133 A6 (l |
| 🟠unknown | low | Session-expired guidance on refresh failure | error_state | On refresh error v2 distinguishes SESSION_EXPIRED and tells the user to log out and back in; otherwise a generic 'Could not refresh servers' destructive toast ( | missing | Tied to the absent refresh action; no equivalent re-auth guidance exists. Low because session expiry surfaces elsewhere via auth flow. |
| 🟢intentional | low | Perspective descriptions and length tiers (brief/detailed/comprehensive) | field | Each perspective lists a human description and selectable length tiers (brief/detailed/comprehensive) each opening the prompt body (Guilds.tsx:178-209). | missing | v3 perspective pills show only a label (Prompts.tsx:91-98); no description and no length tiers exposed in the Prompts view. ADR-133 A2 lists perspectives as shi |
| 🟢intentional | low | Prompt body dialog with file_path + scrollable content | field | Clicking a prompt opens a Dialog with the prompt's name/icon, description, monospace file_path, and the full prompt content in a scroll area (Guilds.tsx:237-281 | missing | No viewer for built-in prompt content/file_path in v3. v3 Prompts dialog only edits user templates (Prompts.tsx:104-138). Covered by the broader 'built-in defau |
| 🟢intentional | low | Page-level error and empty states for the server list | empty_state | v2 shows a dedicated 'Failed to load servers' error block (Guilds.tsx:347-353) and a 'No workspaces found. Connect Discord, Slack, or import WhatsApp chats to g | missing | There is no v3 server-list page, so its load/empty/error states have no counterpart; the workspace <select> just renders the granted set with no empty/onboardin |

### JobsTab → Jobs (web/src/views/Jobs.tsx)  (14 gaps)

_Core shape matches (buckets, type/status filters, expandable per-job details, cost, retry-for-scheduled) and ADR-133 §B/D1 explicitly covers most divergences as intentional; but several real UX gaps remain: no auto-refresh/progress bar, no cancel, raw type labels instead of human titles, and summary/schedule links are now non-navigable mono text._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔴regression | medium | Auto-refresh polling for active jobs | behavior | Jobs list auto-refreshes every 3s while any job is running/pending (JobsTab.tsx:816-823); stats poll every 10s (Jobs.tsx:60). A blue 'N active job(s) - auto-ref | missing | v3 loads once on mount and only updates via the manual Refresh button (Jobs.tsx:46-60). Live job progress is invisible without manual reload. Restore: add a ref |
| 🔴regression | medium | Progress bar + percent + progress message | field | Running/pending jobs render a Progress bar with percent, a progress message, and current_period, e.g. 'Processing… (2024-01) 3/10 (30%)' (JobsTab.tsx:289-304). | changed | v3 only shows a bare 'current/total' count inline (Jobs.tsx:171-175) with no percent, no message, no progress bar, no current_period. The v3 Job type has progre |
| 🔴regression | medium | Navigable links to produced summaries and schedule | navigation | Job cards link to the schedule (/schedules?highlight=) and to produced summaries ('View Summary' / 'View N Summaries' -> /summaries?view=) (JobsTab.tsx:409-443) | changed | v3 renders schedule_name as plain mono text and summary_ids as a comma-joined mono string with no links (Jobs.tsx:194-242). Users cannot navigate from a job to  |
| 🔴regression | low | Distinct loading and error states | loading_state | Separate spinner loading state (JobsTab.tsx:981-985) and a red 'Failed to load jobs. Please try again.' error card (JobsTab.tsx:988-997). | changed | v3 has only a Refresh button label that flips to 'Loading…' (Jobs.tsx:95); on fetch error it catches and sets jobs to [] (Jobs.tsx:50-53), so failures are indis |
| 🟠unknown | medium | Cancel job action | action | Cancel button for running/pending/paused jobs hits POST /jobs/:id/cancel with toast feedback (JobsTab.tsx:483-498, 827-847). | missing | v3 exposes only retryJob in the client (api.ts:413-423); no cancel endpoint/button. ADR-133 D1 (line 140) documents pause/resume as N/A-by-architecture (synchro |
| 🟠unknown | medium | Human-readable job title + description | copy | generateJobTitle/generateJobDescription produce titles like 'Daily Summary' and descriptions like 'Discord · server scope · 3 channels · 2024-01-01 - 2024-01-07 | changed | v3 shows only a raw TYPE_LABEL (e.g. 'Scheduled summary') plus scope (Jobs.tsx:167-170); no synthesized title or descriptive subtitle. ADR-133 §B lists enrichme |
| 🟢intentional | low | Pause / Resume actions | action | Pause button on running jobs and Resume button on paused jobs (POST /jobs/:id/pause\|resume) with 'Job paused/resumed successfully' toasts (JobsTab.tsx:449-481, | missing | ADR-133 §D1 (line 140): pause/resume are 'N/A by architecture' because v3 jobs run synchronously with no executor/queue to suspend; documented in jobs.rs. Inten |
| 🟢intentional | low | Job-parameters summary row on collapsed card | field | Collapsed card shows a params strip: date range, granularity, platform, server name, channel count, cost, and badges Force Regen / No Skip / Per-Channel / lookb | missing | v3 collapsed row carries only type, scope, progress count, cost, time (Jobs.tsx:163-190); fuller params live behind the expand panel. ADR-133 §B enumerates whic |
| 🟢intentional | low | Completion message banner for completed jobs | field | Completed jobs show a green banner with progress.message, e.g. 'Generated 5 summaries' (JobsTab.tsx:307-313). | missing | v3 Job type has no progress message field (types.ts:399-401); 'Produced N summaries' is shown only in the expanded panel (Jobs.tsx:232-242). Acceptable equivale |
| 🟢intentional | low | Pagination / total count indicator | sort_pagination | Shows 'Showing X of Y jobs' when total exceeds the loaded page (limit 50) (JobsTab.tsx:1034-1039). | changed | v3 fetches a flat 200-row limit (Jobs.tsx:50, api.ts:413) and shows only 'N shown' (Jobs.tsx:152) with no total-vs-loaded hint. Higher limit largely sidesteps t |
| 🟢intentional | low | Type/status colored badges and icons | copy | Per-job-type colored badge + icon (Manual/Scheduled/Retrospective/Regenerate/Wiki Backfill) and per-status colored badge + animated icon (JobsTab.tsx:117-132, 2 | changed | v3 keeps a status-colored pill (STATUS_TONE, Jobs.tsx:9-15, 183-189) and a plain-text type label; no type badge color/icon. Cosmetic-leaning; functional status  |
| 🟢intentional | low | Retry available for failed jobs | action | Retry button shown for any failed job, creating a new job via POST /jobs/:id/retry with 'Retry job created' toast (JobsTab.tsx:500-514, 850-873). | changed | v3 offers Retry only when job_type === 'scheduled' (Jobs.tsx:245-260). ADR-133 §D1 (line 140) explicitly scopes retry to re-firing a scheduled job via Summarizi |
| 🟢intentional | low | Create Schedule header CTA | navigation | Header has a 'Create Schedule' button linking to /schedules (Jobs.tsx:121-126). | missing | v3 has a top-level Schedules nav entry (ADR-133 line 47 lists Schedules in nav), so the per-page CTA is reachable elsewhere. Low value. |
| 🟢intentional | low | Extended job metadata in expanded panel | field | Expanded 'Full Job Metadata' dl lists ~30 fields incl. guild_id, summary_type, perspective, category_id, channel_ids, force_regenerate, skip_existing, per_chann | changed | v3 expand panel shows schedule, triggered-by, covered window, duration, failure, pause reason, produced summaries, ID (Jobs.tsx:193-261). The omitted fields are |

### Populate → Knowledge.tsx + RuVector.tsx (Populate-as-screen deferred per ADR-133 A8)  (6 gaps)

_v3 has no dedicated Populate screen; ADR-133 A8 intentionally defers it as "covered by Create/Sync/retrospective," and Knowledge.tsx covers regenerate/curate/search. But several concrete Populate functions (summary→knowledge backfill with progress, ingestion-path settings, allowed-perspectives multi-select, clear-wiki, clear-ruvector) have no v3 equivalent on any surface._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟢intentional | medium | Backfill from existing summaries (mode unprocessed/all) with live progress, ingested/skipped/failed stats, cancel, and paused-job recovery | action | Populate.tsx:204-242,484-633 — startBackfill POST /wiki/backfill with mode/batch_size/update_threshold; status polled every 3s (lines 190-198); progress bar + i | missing | ADR-133 A8 (lines 8-9,96): 'Populate is satisfied by the existing manual-ingest surfaces (Create→Past / retrospective by-week / source Sync) rather than a redun |
| 🟠unknown | medium | Ingestion-path settings: Wiki Pages auto-ingest toggle + RuVector Knowledge Units toggle (wiki_auto_ingest / wiki_ingest_to_vectors) | field | Populate.tsx:360-398 — two Switches PATCHing /wiki/settings {wiki_auto_ingest}, {wiki_ingest_to_vectors}; controls what gets populated when new summaries are cr | missing | No grep hit for wiki_auto_ingest/ingest_to_vectors anywhere in web/src. ADR-133 A8 does not enumerate these per-path ingestion toggles as covered; only the back |
| 🟠unknown | low | Allowed Perspectives multi-select (clickable badges with per-perspective counts; at-least-one validation) | filter | Populate.tsx:400-445 — perspective badges from /wiki/available-perspectives with counts (407-419); togglePerspective PATCHes wiki_allowed_perspectives (177-187) | missing | No allowed_perspectives handling in web/src. ADR-133 ships Prompts/Perspectives (A2) for selecting perspective per schedule/summary, but not a per-workspace fil |
| 🟠unknown | low | Clear Wiki destructive action (delete all pages/sources) with two-step confirm and count preview | action | Populate.tsx:126-128,244-264,637-686 — DELETE /guilds/{id}/wiki; inline confirm shows 'delete N pages and M sources' (662), disabled when no pages or backfill a | missing | No clearWiki in web/src. Knowledge.tsx offers Prune (dedup, Knowledge.tsx:93-107) but no full wipe. Not listed as covered/deferred in ADR-133. Restore hint: add |
| 🟠unknown | low | Clear RuVector destructive action (delete all knowledge units/edges/learning signals) with two-step confirm | action | Populate.tsx:130-132,266-284,688-738 — DELETE /ruvector/guilds/{id}/clear?confirm=true; inline confirm (713); disabled while backfill active (707); copy notes n | missing | RuVector.tsx is explicitly read-only (RuVector.tsx:5-7: 'a read-only browser over the knowledge vector store'). No clear action. ADR-133 A7 ships RuVector as an |
| 🔵superseded | low | Wiki statistics card (Total Pages, Sources, Topics, Decisions) with loading skeletons and empty state | screen | Populate.tsx:144-148,306-345 — fetchWikiStats /wiki/stats; 4-stat grid (323-339); Skeleton loading (315-321); 'No wiki data yet' empty state (342) | changed | v3 surfaces equivalent counts elsewhere: Knowledge.tsx:145-148 shows unit_count + updated_at on the synthesized page; RuVector.tsx:38-46 shows Units/Embedded/Mo |

### PromptTemplates → Prompts.tsx  (11 gaps)

_v3 Prompts ships the core CRUD + built-in perspectives + usage count, but drops description, based-on-default seeding/badge, the Duplicate action (ADR-deferred), the View-Usage detail dialog, the force-delete-warning when in use, and several inline-copy/empty-state/loading affordances._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔴regression | medium | Description field (create/edit form + card display) | field | v2 form has an optional Description input (PromptTemplates.tsx:369-379) persisted via based_on/description and shown on each card (PromptTemplates.tsx:541-545). | missing | v3 editor only has name+content (Prompts.tsx:109-121) and the v3 PromptTemplate type has no description field (types.ts:209-217). Restore: add description to ty |
| 🔴regression | medium | "New template" templates: Start-from-scratch + Based-on-default seeding | action | v2 New Template is a dropdown with 'Start from scratch' plus one item per built-in default ('Based on: {name}'), seeding name/content from the chosen default (P | missing | v3 'New template' is a single button opening a blank editor (Prompts.tsx:76-82). The v3 API createPrompt already accepts based_on (api.ts:510) and the type trac |
| 🔴regression | medium | Delete confirmation with in-use warning + force delete | validation | v2 delete AlertDialog shows a distinct message when usage_count>0 ('used by N schedule(s)… will remove the template reference') and forces force=true on delete  | changed | v3 remove() deletes immediately with no confirmation dialog and no in-use warning (Prompts.tsx:59-63,160-165). Even an unconfirmed delete of an in-use template  |
| 🔴regression | low | "Based on: {default}" badge on template card | field | v2 card shows a secondary badge 'Based on: {based_on_default}' when set (PromptTemplates.tsx:579-583). | missing | v3 card row (Prompts.tsx:148-169) never renders based_on although the field exists on the v3 type (types.ts:213). Add a badge/label when t.based_on is set. |
| 🔴regression | low | 'Updated {relative time}' timestamp on card | field | v2 card shows 'Updated {formatRelativeTime(updated_at)}' (PromptTemplates.tsx:591-593). | missing | v3 PromptTemplate type carries updated_at (types.ts:216) but the card never renders it (Prompts.tsx:148-169). Add a relative-time line. |
| 🟢intentional | low | Duplicate template action | action | v2 offers per-card Duplicate via the kebab menu and a dedicated dialog with an editable new-name field (PromptTemplates.tsx:227-249,461-508,558-561). | missing | ADR-133 D5 (line 144) explicitly lists 'Duplicate prompt template' as low value: 'do last or skip.' Documented deferral, not a regression. |
| 🔵superseded | low | View Usage dialog (lists schedules using the template) | action | v2 has a 'View Usage' kebab action opening a dialog that fetches and lists the schedules referencing the template, with its own loading skeleton and empty state | changed | v3 reduces usage to a static 'used {n}×' count on the card (Prompts.tsx:153). ADR-133 A2 specifies only 'each shows how many schedules use it' (line 69-70), so  |
| 🟠unknown | low | Inline copy: 'JSON output / citation auto-appended' help text | copy | v2 shows helper copy under the content textarea explaining JSON output format and citations are auto-appended so the author need not include them (PromptTemplat | missing | v3 editor has only placeholder text (Prompts.tsx:115-121), no such guidance. If the v3 backend still auto-appends format/citation instructions, restore the hint |
| 🔵superseded | low | Loading skeleton state | loading_state | v2 renders a structured TemplatesSkeleton (header + 3 card skeletons) while isLoading (PromptTemplates.tsx:251-253,649-673). | changed | v3 has only a busy flag toggling button text ('Saving…') (Prompts.tsx:128); initial list load shows no skeleton/spinner, just empty until data arrives. Minor; a |
| 🔵superseded | low | Empty-state guidance (built-ins list + suggested use cases + CTA) | empty_state | v2 empty state names the built-in perspectives, suggests use cases (Standup, Security, Product), and includes a 'Create Template' CTA (PromptTemplates.tsx:292-3 | changed | v3 empty state is a single muted line 'No templates yet — create one…' (Prompts.tsx:142-145). Built-in perspectives are shown separately as chips at top (Prompt |
| 🔵superseded | low | Per-item error toasts vs single shared error banner | error_state | v2 surfaces success and granular failure toasts for create/update/delete/duplicate with server error messages (PromptTemplates.tsx:155-166,190-200,212-224). | changed | v3 uses one inline error banner with generic strings ('Save failed.', 'Could not load prompts.') and no success feedback; delete failures are unhandled (Prompts |

### RuVector tabs, search, source references, recent changes, browse+filters, ratings, and admin regeneration controls) → Knowledge.tsx (plus RuVector.tsx for the vector-store explorer)  (15 gaps)

_v3 deliberately collapses v2's multi-page categorized wiki into a single synthesized "Knowledge base" page + semantic search + curator (ADR-127 v1 scope); most v2 wiki structure (page tree, per-page views, categories, ratings, recent-changes, source-reference view, populate/backfill/regeneration controls) is intentionally not ported, but several still-relevant affordances (provenance drill-through, dirty-page regeneration, per-page sharing) are absent without explicit ADR coverage._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟢intentional | medium | Per-page detail view with Synthesis / Updates / RuVector tabs, source-titled update sections, and inline source links | screen | WikiPageView renders tabbed page (synthesis/updates/ruvector) with markdown, source-title rewriting and [source:xxx] link processing (Wiki.tsx:628-1197, process | missing | ADR-127 (line 92) ships one synthesized Knowledge-base markdown page (WikiPage.content_md, types.ts:386-390) rather than many addressable pages. No per-page rou |
| 🟢intentional | medium | Wiki search with AI synthesized answer and knowledge-gap detection | search | WikiSearch posts to /wiki/search?synthesize=true and renders an 'AI Answer' synthesis card plus a 'Knowledge Gaps Detected' alert listing gaps (Wiki.tsx:187-189 | changed | v3 Knowledge search (Knowledge.tsx:53-64,274-322) returns ranked knowledge-unit hits with %-scores and source counts (KNO-005, ADR-127) but has no synthesized A |
| 🟢intentional | medium | Wiki regeneration controls: full Regenerate Wiki job with progress, dirty-page-count Regenerate, auto-synthesis toggle, mark-all-dirty admin action | action | Header controls: Regenerate Wiki (scope:full job with processed/page progress), dirty-page Regenerate with count badge + last-run tooltip, auto-synthesis Switch | changed | v3 collapses these to a single synchronous Generate/Regenerate button that re-synthesizes the whole knowledge base (Knowledge.tsx:66-77,133-139). No auto-synthe |
| 🟢intentional | low | Hierarchical category navigation tree (topics / decisions / processes / experts / questions) with per-category page counts and per-page links | navigation | WikiNavTree renders a sidebar tree of categories with icons + page-count badges and links to each child page (Wiki.tsx:525-571, categoryIcons 533-539); fed by f | missing | ADR-127 v1 (lines 54, 92) scopes wiki to a single synthesized page from keyword/tag clustering over units, not a navigable category tree. ADR-133 A7 only commit |
| 🟢intentional | low | Source-reference view: open a source to see its summary metadata (platform, date range, scope, channels, key points) and all wiki pages that cite it | screen | SourceReferences (rendered when ?source= is set) shows rich summary metadata card + 'View Summary' link + 'Pages using this source' list (Wiki.tsx:1434-1647); l | missing | v3 provenance is expressed as per-unit source_ids counts (Knowledge.tsx:313-317, RuVector.tsx:78-80) not a dedicated source-reference drill-through screen. ADR- |
| 🟢intentional | low | Recent Changes feed of recently edited wiki pages | screen | RecentChanges component lists pages by changed_at with click-through, default tab on the wiki landing (Wiki.tsx:1650-1698, 2285-2296); GET /wiki/recent (Wiki.ts | missing | No page-level change history in v3's single-page model. Not called out in ADR-127/133; superseded by the single regenerable Knowledge page whose updated_at is s |
| 🟢intentional | low | Browse view with shared filters (min sources 2+/5+/10+, has-synthesis, has-RuVector, min rating 3+/4+/5+) and sort options (updated/created/sources/rating/title) | filter | WikiBrowser + sidebar filter panel: source-count, synthesis, ruvector, min-rating filters, sort select, clear-filters, filtered/total count (Wiki.tsx:1700-1765, | missing | v3 has no multi-page list to filter/sort — the knowledge base is one synthesized page (ADR-127 line 92). Filters/sort are moot in the v1 model. |
| 🟢intentional | low | Per-page 5-star rating of synthesis with average + count display | action | StarRating + ratingMutation POST /wiki/pages/:path/rate; synthesis footer shows current average rating and a Rate control (Wiki.tsx:585-626, 706-718, 994-1011) | missing | ADR-127 v1 scope (semantic search, coherence, provenance, single wiki page) does not include synthesis ratings; no rating field on v3 WikiPage (types.ts:386-390 |
| 🟢intentional | low | Per-page Share action copying a deep link (including active tab) | action | handleShare copies page URL with ?tab= param to clipboard with toast feedback (Wiki.tsx:737-759, 904-916) | missing | No per-page URLs exist in v3's single-tab Knowledge view, so a per-page share link is not applicable. Consequence of the intentional single-page model. |
| 🟢intentional | low | Synthesis metadata footer: auto-synthesized badge, model used, synthesized-at timestamp, source count | field | Synthesis footer shows auto_synthesized badge, synthesis_model (anthropic/ stripped), synthesis_updated_at tooltip, and source count (Wiki.tsx:956-993) | changed | v3 shows a reduced footer: unit_count + page.updated_at only (Knowledge.tsx:145-148). No model/auto-synthesized indicator. Consistent with ADR-127's simpler pag |
| 🟢intentional | low | Per-page related-pages section (links-to / linked-from) and page-stat cards (sources, links, updated, confidence) and topic chips | field | WikiPageView renders Related Pages (linked_pages_from/to), 4 stat cards including Confidence %, and topic badges (Wiki.tsx:1199-1299) | missing | Inter-page links, per-page confidence, and topic chips presuppose the multi-page model absent in v3 (single content_md page, types.ts:386-390). Not in ADR-127 v |
| 🟢intentional | low | RuVector backfill / single-page processing controls (process page into KUs, bulk backfill with coverage %) | action | ruvectorMutation processes a page into KUs (Wiki.tsx:720-735, 'Re-extract KUs'); header RuVector backfill button shows units badge + coverage tooltip (Wiki.tsx: | changed | ADR-133 A7 scopes the v3 RuVector surface to a READ-ONLY explorer (RuVector.tsx:5-7 header comment, GET vectors). KU extraction happens automatically during sum |
| 🟢intentional | low | RuVector tab breakdown of units by type (Key Facts/claims, Decisions, Action Items with checkboxes, Questions) per page | screen | Per-page RuVector tab groups units into claim/decision/action_item/question cards with counts and per-item rendering (Wiki.tsx:1040-1188) | changed | v3 RuVector Explorer lists units flat with kind badge, embedding/model info, and source channel (RuVector.tsx:60-87), not grouped per-page by unit type. ADR-133 |
| 🔵superseded | low | Browser tab title set to wiki page / source title | behavior | useEffect sets document.title to page/source title while viewing (Wiki.tsx:636-639, 1451-1455) | missing | No per-page view in v3 to title; the single Knowledge tab uses the app-level title. Follows from the single-page model, not an independent regression. |
| 🟢intentional | low | Knowledge units export (.rvf / JSON) — present in v3, absent in v2 | action | v2 Wiki has no unit export; v3 adds Export .rvf and JSON buttons (Knowledge.tsx:24-43,228-243) per ADR-117 | present | Not a gap — a v3 addition where the rewrite exceeds v2 (ADR-117). Recorded for completeness of the comparison. |

### RuVectorExplorer → RuVector (web/src/views/RuVector.tsx) — semantic search + RVF export live in the Knowledge view (web/src/views/Knowledge.tsx)  (11 gaps)

_v3 ships an ADR-133 A7 read-only vector-store browser: stats (units/embedded/models) plus a unit list with embedding metadata. v2's tabbed Explorer (Semantic Search, Browse, Graph) is reduced — semantic search and RVF export still exist but moved to the Knowledge view; the Knowledge Graph tab, edge/signal stats, and per-type bar breakdowns are dropped (largely intentional per ADR-133 "optional/debug")._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟠unknown | medium | Unit-type filter (claim/decision/question/action_item/context/definition/reference) for browsing units | filter | Both Search and Browse tabs offer a Select to filter units by type, querying the backend with unit_type/unit_types (RuVectorExplorer.tsx:344-358, :422-436, :108 | missing | v3 RuVector lists units with their kind shown (RuVector.tsx:71) but offers no filter by type; client.vectors() takes no type param. Knowledge view also has no u |
| 🟠unknown | medium | Result limit / page-size selector (25/50/100/200) for browsing units | sort_pagination | Browse tab Select sets the fetch limit and shows 'Showing N of total' (RuVectorExplorer.tsx:437-447, :486-490). | missing | v3 RuVector renders all units returned by client.vectors() with no limit control or 'showing N of total' affordance (RuVector.tsx:66-86). No pagination at all;  |
| 🟢intentional | low | Knowledge Graph tab (interactive node/edge visualization, ADR-093) | screen | Third tab renders a lazy-loaded KnowledgeGraph component visualizing units and relationships (RuVectorExplorer.tsx:44, :547-595). | missing | ADR-133 (docs/adr/ADR-133-...md:95, :158, :169) ships A7 as a read-only browser only; RuVector/graph explicitly 'optional/debug; do last' and a non-goal. No Kno |
| 🟢intentional | low | Semantic search within the RuVector view | search | SearchTab: query input + type filter, hits show %-match and search_time_ms, with empty/error/loading states (RuVectorExplorer.tsx:313-407). | changed | Satisfied by equivalent: semantic search lives in Knowledge view (Knowledge.tsx:274-322, KNO-005) with score and provenance. RuVector itself (RuVector.tsx) is r |
| 🟢intentional | low | RVF / JSON export within the RuVector view | action | Browse tab has an 'Export RVF' button opening ExportDialog (RuVectorExplorer.tsx:449-463). | changed | Satisfied by equivalent: Export .rvf and JSON buttons live in Knowledge view (Knowledge.tsx:228-243, ADR-117). v3 uses direct download buttons rather than a con |
| 🟠unknown | low | Per-type unit breakdown (units_by_type) bar chart | field | UnitTypeBreakdown card renders a sorted bar chart of counts per unit type with icons (RuVectorExplorer.tsx:188-231, stats.units_by_type). | missing | v3 shows only Units/Embedded/Models totals (RuVector.tsx:38-46); no per-type distribution. Vectors type carries no units_by_type field. Not explicitly addressed |
| 🟢intentional | low | Edge stats (total_edges, edges_by_type breakdown) | field | StatsOverview shows total Edges; EdgeTypeBreakdown card renders edges_by_type bar chart (RuVectorExplorer.tsx:146-157, :233-273). | missing | Graph/edge data is part of the dropped knowledge-graph feature (ADR-133:158,169 — RuVector optional/debug). v3 Vectors type exposes no edge data. |
| 🟠unknown | low | Signals stat (total_signals — feedback signals collected) | field | StatsOverview card shows total_signals with 'Feedback signals collected' (RuVectorExplorer.tsx:172-183). | missing | Not present in v3 Vectors type or RuVector view. No ADR explicitly defers the signals metric. |
| 🟠unknown | low | Per-unit metadata: source date and match score on unit cards | field | KnowledgeUnitCard shows source_date (formatted) and %-match score alongside type and channel (RuVectorExplorer.tsx:294-305). | changed | v3 unit rows show kind, embedding dims/model or 'unembedded', and source_channel (RuVector.tsx:67-82) but no source_date or match score. v3 adds embedding model |
| 🟢intentional | low | Embeddings coverage percentage stat | field | StatsOverview computes and shows embedding coverage as a percentage plus 'N with vectors' (RuVectorExplorer.tsx:127-129, :159-170). | changed | v3 shows 'embedded/total' as a fraction with amber/emerald tone instead of a percent (RuVector.tsx:40-44). Equivalent information, different presentation — func |
| 🟠unknown | low | Per-stat-card loading skeletons | loading_state | Stats and breakdown cards render Skeleton placeholders while loading (RuVectorExplorer.tsx:522-528, :601-605); Browse shows row skeletons (:465-470). | missing | v3 RuVector shows nothing until data resolves (RuVector.tsx:36 gated on data) — no loading state, only an error message on failure. |

### ScheduleCard → Schedules (web/src/views/Schedules.tsx)  (14 gaps)

_v3 covers ADR-133 §B steering parity (perspective/template/title/continuity) plus scope/rolling/destinations, and adds an inline creation form v2 delegated to a wizard; but the v3 schedule list/cards are markedly thinner than v2 — no schedule name, no run/failure/last-run stats, no missed/catch-up affordance, no delete confirmation, no per-run error detail, and no weekly-day support._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟠unknown | medium | Per-schedule run/failure/last-run statistics on the schedule card | field | ScheduleCard shows "Runs: {run_count}", "Failures: {failure_count}", "Last: {relative last_run}", and "Next: {relative next_run}" (ScheduleCard.tsx:197-210) | missing | v3 list shows only enabled/paused and next-run absolute time (Schedules.tsx:292-308). v3 Schedule type lacks run_count/failure_count/last_run (it has consecutiv |
| 🟠unknown | medium | Missed-run detection + prominent "Catch Up" action | action | When next_run is in the past and active, ScheduleCard shows a destructive "Missed" badge and a prominent orange "Catch Up" button instead of the plain Run-now ( | missing | v3 always shows a plain "Run now" button (Schedules.tsx:311-316) and never flags missed schedules. v3 has next_run available to compute this. Not addressed in A |
| 🟠unknown | medium | Delete confirmation dialog | validation | Delete is gated behind an AlertDialog confirming "Are you sure you want to delete '{name}'? This action cannot be undone." (ScheduleCard.tsx:346-370) | changed | v3 deletes immediately on clicking the 🗑 button with no confirmation (Schedules.tsx:329-334). Add a confirm step to prevent accidental destructive action. |
| 🟠unknown | medium | Per-run error/detail text in run history | field | RunHistoryDrawer renders execution.error in red and a computed run duration per execution (RunHistoryDrawer.tsx:140-155) | changed | v3 inline run list shows only status, (manual), and ran_at (Schedules.tsx:344-352) and never renders ScheduleRun.detail, which carries the failure/info message  |
| 🟠unknown | low | Schedule name / title on each card | field | ScheduleCard headers each schedule with its {schedule.name} (ScheduleCard.tsx:131) and delete dialog references it | missing | v3 labels each schedule by type + time + timezone only (Schedules.tsx:288-291); the v3 Schedule type has no name field (types.ts:82-107). title_template exists  |
| 🟠unknown | low | Collapsible help note: Normal vs Rolling schedules | copy | A dismissible info Card explains Normal vs Rolling schedules with examples and where to enable rolling (Schedules.tsx:139-173) | missing | v3 has only a one-line inline hint when a rolling period is chosen (Schedules.tsx:192-196). The explanatory contrast between normal and rolling is absent. Add b |
| 🟠unknown | low | Weekly schedule type + day-of-week selection/display | field | ScheduleCard handles schedule_type === 'weekly' and renders the selected day names, e.g. (Mon, Wed) (ScheduleCard.tsx:183-187) | changed | v3 creation form offers only Hourly/Daily (Schedules.tsx:105-106) and never renders schedule_days, though the v3 Schedule type carries days[] and day_of_month ( |
| 🟠unknown | low | Typed destination chips on the card | field | ScheduleCard renders typed destination badges with icons/labels (Dashboard/Discord/DM/Webhook/Email/Confluence) for each pinned destination (ScheduleCard.tsx:27 | changed | v3 list shows only a count: "→ N destinations" or "→ all destinations" (Schedules.tsx:305-307). The creation form does have a destination multi-select with kind |
| 🟠unknown | low | Rolling-summaries display on rolling schedules (current period + previous + view-all) | screen | ScheduleCard fetches and shows the current rolling summary (day X of Y, rollover date, message count) plus previous finalized summaries and a "View all N summar | missing | v3 only flags rolling via a text fragment "📅 rolling {period}" (Schedules.tsx:300). The rolling-summary browser is absent. ADR-133 §B lists rolling under Schedu |
| 🟠unknown | low | Loading skeleton for the schedules list | loading_state | SchedulesSkeleton renders 3 placeholder cards while isLoading (Schedules.tsx:175-176,233-260); RunHistoryDrawer also has a skeleton (RunHistoryDrawer.tsx:100-11 | missing | v3 has no loading state; load() populates state with no pending indicator (Schedules.tsx:33-48). A brief skeleton or spinner would avoid an empty flash. |
| 🟠unknown | low | Copy-Schedule-ID affordance | action | ScheduleCard exposes a click-to-copy schedule ID with copied checkmark feedback (ScheduleCard.tsx:114-118,211-222) | missing | v3 never surfaces the schedule id (Schedules.tsx:284-308). Minor operator convenience. |
| 🟠unknown | low | Edit schedule action | action | ScheduleCard has a pencil Edit button that opens the create/edit wizard for the schedule (ScheduleCard.tsx:324-331; Schedules.tsx:60-62) | missing | v3 has no edit affordance — a schedule can only be created, run, paused/resumed, or deleted (Schedules.tsx:310-334). To change a schedule the user must delete a |
| 🟠unknown | low | View-summary navigation from a run | navigation | RunHistoryDrawer renders a "View" button on executions with a summary_id, navigating to the produced summary (RunHistoryDrawer.tsx:80-84,157-167) | missing | v3 inline run rows are non-interactive (Schedules.tsx:344-352); there is no link from a run to its produced summary. v3 ScheduleRun has no summary_id field (typ |
| 🔵superseded | low | Run-now result feedback | behavior | v2 uses toasts for run/delete/error outcomes (Schedules.tsx:100-114, 84-98) | changed | v3 shows an inline transient note that is more informative than v2's toast — it distinguishes "Ran — summary produced" vs "Ran — no messages in the window, noth |

### ShareReceived → (none) — closest is web/src/views/Whatsapp.tsx, but it is a manual file-upload form, not the OS share-target entry point  (8 gaps)

_v2's PWA share-target page (receive a WhatsApp export shared from another app → pick workspace → upload) has no v3 counterpart; v3 only offers a manual file-picker upload on the Whatsapp view, and ADR-133/134 do not document the share-target flow as a deliberate deferral._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟠unknown | high | PWA OS-share-target entry path (receive a file shared from WhatsApp/another app and land on a dedicated import page) | navigation | ShareReceived is a dedicated route that reads a file stashed by a service worker in IndexedDB ('SummaryBotShare' store, 'sharedFiles'/'pending' key) and drives  | missing | No PWA in v3: no web manifest, no service worker, no share-target handler, no route. grep for share_target/serviceWorker/SummaryBotShare in web/src finds nothin |
| 🔵superseded | low | Workspace (guild) selection at import time | field | After a file is received, a 'Select workspace' labeled Select dropdown populated from GET /guilds lets the user choose the destination workspace before upload;  | changed | v3 has no per-import workspace picker; the import always targets the session's active workspace, switched via the global nav workspace switcher (auth.tsx:58-69, |
| 🟠unknown | low | 'Loading shared file…' state | loading_state | Spinner + 'Loading shared file...' while the SW-stashed file is read from IndexedDB; ShareReceived.tsx:233-238 | missing | No equivalent because the share-receive flow does not exist in v3. v3 Whatsapp.tsx has only an 'Importing…' busy state for manual upload (Whatsapp.tsx:129). |
| 🔵superseded | low | 'Not authenticated' state with Go-to-Login CTA | empty_state | If the user is not authenticated when a file is shared, shows a warning icon, 'Please log in to import WhatsApp chats', and a 'Go to Login' button; ShareReceive | missing | v3 gates the whole dashboard behind login (App.tsx renders Login when unauthenticated), so an unauth share landing isn't a distinct screen. Only matters if the  |
| 🟠unknown | low | 'No file to import' empty state | empty_state | When the route is opened but no shared file is found in IndexedDB, shows a file icon, 'No file to import. Share a WhatsApp export to this app.', and a 'Go to Da | missing | No share-target route exists in v3, so this guidance has no home. Restore alongside the share-target view if reinstated. |
| 🔵superseded | low | Post-import success state with message count and 'View Import' CTA | empty_state | On success shows a green check, 'Import successful!', '<n> messages imported', and a 'View Import' button that navigates to /guilds/:id/whatsapp; ShareReceived. | changed | v3 surfaces an equivalent inline result on the manual Whatsapp view: 'Imported <stored> messages into #<chat_id>' plus duplicates/participants/format and a 'Sum |
| 🔵superseded | low | Error state with message and recovery CTA | error_state | Destructive Alert showing the failure message (URL ?error param, IndexedDB read failure, or upload failure) plus a 'Go to Dashboard' button; ShareReceived.tsx:3 | changed | v3 manual upload shows an inline error 'Import failed (<status>): <msg>' (Whatsapp.tsx:53-58,133). Equivalent for manual upload; the share-specific error source |
| 🟠unknown | low | Crash error boundary with diagnostic stack + recovery | error_state | A React class ErrorBoundary wraps the page and, on crash, renders the error message + full stack and a 'Go to Dashboard' button; ShareReceived.tsx:26-71,91-97 | missing | v3 Whatsapp.tsx has no error boundary; an uncaught render error would propagate. Whether this matters globally depends on a top-level boundary in App.tsx (not v |

### SlackWorkspaces → Source (rendered with platform="slack"; App.tsx:189)  (9 gaps)

_v3 replaces v2's named-workspace OAuth model with a generic bot-token source-ingestion form (intentional, ADR-133:146-147). Real gaps: no per-channel auto-summarize/sensitive settings, no persistent workspace list/disconnect, and no Slack-not-configured guidance._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟠unknown | high | Per-channel auto_summarize and is_sensitive toggles | field | Channel list lets a user toggle per-channel auto_summarize and is_sensitive (mark sensitive to exclude from cross-channel summaries) via Switches calling onUpda | missing | v3 SourceChannel (types.ts:514-523) has no auto_summarize/is_sensitive fields and the channel browser (Source.tsx:330-368) is select-to-sync only. Auto-summariz |
| 🟢intentional | medium | Persistent workspace list with metadata (name, domain, enterprise/scope/active badges, channel count, last-synced / connected timestamps) | screen | Connected workspaces render as cards showing workspace_name, domain, Enterprise/scope/Active-or-Disabled badges, channel count, and relative last_sync_at / inst | missing | Consequence of the bot-token model (ADR-133:146-147): v3 stores a single token + connection status (types.ts:452-455), not a list of named workspaces. No per-wo |
| 🟢intentional | low | OAuth connect flow: link to Discord server (guild) + scope-tier picker (Public/Full) + requested-scopes preview | action | Connect dialog runs OAuth: select a guild, choose 'public' vs 'full' scope tier as cards, and preview the requested Slack scopes before continuing (SlackWorkspa | missing | ADR-133:146-147 records the Slack OAuth install flow as a known deferral, with the bot-token path (Source.tsx:226-255) as the working equivalent. No restore nee |
| 🟢intentional | low | Disconnect workspace action with confirmation dialog | action | Per-workspace Disconnect button opens an AlertDialog confirming removal and noting summaries are preserved (SlackWorkspaceCard.tsx:182-218; handler SlackWorkspa | changed | v3 has a 'clear' token action (Source.tsx:104-108, 234-236) instead of named-workspace disconnect, consistent with the bot-token model (ADR-133:146-147). No con |
| 🟢intentional | low | Channel search box, show-archived filter, and public/private grouping with stats | filter | Channel sheet provides a search input (with clear button), a 'Show archived' switch, public/private channel grouping, and a stats line (#public / #private / #au | changed | v3 channel browser groups by category and shows a 'N selected' count (Source.tsx:330-366) but has no name search, no archived filter, and no public/private/auto |
| 🟠unknown | low | Per-channel metadata: member count, topic, Archived/Shared badges, channel-type icon (public/private/dm/mpim) | field | Channel rows show member_count, topic, Archived and Shared badges, and a type-specific icon (Hash/Lock/MessageSquare/Users) (SlackChannelList.tsx:83-88, 197-245 | missing | v3 SourceChannel (types.ts:514-523) carries only id/name/category/accessible; no member_count/topic/archived/shared/type. Not addressed by a cited ADR; minor in |
| 🟢intentional | low | 'Slack Integration Not Configured' admin warning (missing SLACK_CLIENT_ID/SECRET) | empty_state | When slackStatus.configured is false, an amber banner explains the Slack app credentials are unset and to contact an admin to set SLACK_CLIENT_ID/SECRET; the Co | changed | OAuth-credential config is moot under the bot-token model. v3's analog is the 'supported' check: if the API lacks the slack feature it shows a rebuild hint (Sou |
| 🔵superseded | low | Sync workspace action with synced-counts toast (channels + users) | action | Per-workspace Sync button triggers a sync and toasts 'Synced N channels and M users' (SlackWorkspaceCard.tsx:152-166; handler SlackWorkspaces.tsx:115-131). | changed | v3 'Sync now' (Source.tsx:110-133, 387-393) reports fetched/stored/channel counts and per-channel errors inline (Source.tsx:398-423) — a richer result surface t |
| 🟢intentional | low | 'Public channels only' scope warning prompting reconnect with Full Access | copy | When a workspace uses the public scope tier and no private channels are present, an amber panel explains the limitation and that reconnecting with Full Access i | missing | No scope-tier concept in v3 (token grants whatever the bot's Slack scopes allow). Tied to the deferred OAuth flow (ADR-133:146-147). v3 instead flags per-channe |

### Sources → Source.tsx (per-platform connect/sync; rendered as Sources > discord & Sources > slack tabs; WhatsApp split into Whatsapp.tsx)  (11 gaps)

_v2's "All Sources" is a read-only unified aggregation (Discord channels + WhatsApp imports + Slack channels in one list, with platform filter, search, sort, status, and quick-links); v3 has no such aggregated listing — Sources is split into three per-platform connect/sync tabs. The split is an intentional architectural decision (ADR-133 nav restructure + A8), but the specific cross-platform listing/filter/search/sort/status affordances have no v3 equivalent._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟢intentional | medium | Unified cross-platform source list (one combined list of Discord channels + WhatsApp imports + Slack channels) | screen | Builds a single UnifiedSource[] merging guild.channels (Discord), archive whatsapp: sources, and linked slack: sources/channels into one rendered list. Sources. | missing | ADR-133 restructures the v2 'All Sources' screen into per-platform tabs (Sources(WhatsApp · Discord · Slack), ADR-133:47-48,212-render at App.tsx:187-189) and A |
| 🟢intentional | medium | Per-source enabled/disabled status indicator (green check vs grey X) | field | Each source row shows CheckCircle2 (enabled) or XCircle (disabled); Discord enabled state derives from guild.config.enabled_channels.includes(channel.id). Sourc | missing | v3 has no source-listing row to carry status. Channel enablement/accessibility is surfaced differently: the v3 directory marks channel accessibility (🔒 no acces |
| 🟢intentional | medium | SlackLinkManager — link/unlink Slack workspaces to a guild (many-to-many sharing, ADR-085) | action | Header dialog lists linked workspaces (with unlink, Primary badge), available workspaces to link with an 'Enable summarization' toggle, and per-workspace domain | changed | v3 models Slack via a bot-token connect + server picker on the Slack tab (Source.tsx:226-291) under the ADR-120 multi-workspace-source model. There is no UI to  |
| 🟢intentional | low | Platform filter buttons (All / Discord / WhatsApp / Slack) with per-platform counts | filter | Filter row with a button per platform, each showing a count Badge from platformCounts, filtering the unified list. Sources.tsx:179-187 (counts), 220-244 (button | missing | Follows from the unified-list removal: with separate per-platform tabs (App.tsx:187-189,212) a platform filter is structurally redundant. No restore needed unle |
| 🟢intentional | low | Search box across all sources by name | search | Text Input filters the unified source list case-insensitively by source name. Sources.tsx:78,156-160,247-256 | missing | Only meaningful against the unified aggregated list, which v3 intentionally does not have (ADR-133). v3 Source.tsx instead offers a channel-directory browser pe |
| 🟢intentional | low | Sort control (By Platform / By Name / By Status) | sort_pagination | Select drives sortBy applied to the unified list with tie-breaking on name; status sort orders enabled-first. Sources.tsx:79,162-174,257-266 | missing | Tied to the unified list (ADR-133 divergence). v3's channel directory is grouped-by-category and name-sorted instead (Source.tsx:331-362,465-480). |
| 🟢intentional | low | "Imported" badge for archive-sourced (WhatsApp) entries | field | Sources flagged isArchived render an 'Imported' badge with an Upload icon in the row. Sources.tsx:111(isArchived true for whatsapp),324-329 | missing | No unified row to host the badge; WhatsApp imports live on the dedicated Whatsapp.tsx tab where import provenance is implicit. Tied to the unified-list divergen |
| 🟢intentional | low | Per-platform/workspace type sub-badge on a source (e.g. Slack workspace name, Discord channel type) | field | Row renders source.type as a secondary Badge — Discord channel type, or Slack workspace/server name. Sources.tsx:95(discord type),126(slack workspace as type),3 | missing | Consequence of removing the unified row. Slack workspace identity is instead managed on the Slack tab / server picker (Source.tsx:278-291). No restore needed. |
| 🟢intentional | low | Empty-state messaging with context-aware copy and WhatsApp import CTA | empty_state | When the filtered list is empty, shows a Layers icon and copy that varies by whether a search is active, a platform filter is active, or nothing is configured;  | missing | No unified list means no aggregated empty state. v3's per-platform tabs provide their own contextual guidance (e.g. Source.tsx:293-297 'No servers — invite the  |
| 🟢intentional | low | Quick-link cards (Configure Discord Channels / Import WhatsApp / Connect Slack Workspace) | navigation | Three navigation Cards linking to the channels config, WhatsApp archive import, and Slack connect pages, each with icon + description. Sources.tsx:349-398; head | changed | These cross-screen jump-offs are subsumed by v3's top-level Sources nav tabs themselves (whatsapp/discord/slack — App.tsx:187-189,212), so each destination is o |
| 🟢intentional | low | Loading skeleton tailored to the sources layout | loading_state | SourcesSkeleton renders header + filter-button + list skeletons while guild/archive load. Sources.tsx:189-191,403-432 | changed | v3 Source.tsx loads connection status async (Source.tsx:82-87) and gates controls on tokenSet rather than showing a list skeleton; different shape because there |

### Summaries → Summaries.tsx (list/compose/filters/facets/calendar); generation wizard split into CreateSummary.tsx  (31 gaps)

_v3 covers the core list, compose, calendar, facets (kind/perspective), and per-summary regenerate/pin/archive/delete/publish, but is markedly thinner on filtering breadth (source/scope/platform/length/granularity/continuity/rolling/schedule, content-count, sort), bulk publish/regenerate UX, per-summary push-to-channel/DM/email, server-side pagination, and the wizard's rich generation options (perspective/custom-template, summary length, scope, custom date range). Several gaps are explicitly intentional per ADR-133._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟠unknown | medium | Created date-range filter with presets | filter | Date-range popover (created_after/before) with Today/7d/30d/90d presets and dual calendars (SummaryFilters.tsx:430-474,1137-1216) | missing | v3 only offers free-text q/participant/tag/archived (Summaries.tsx:169-228). No date filtering at all on the list; the Calendar view shows only the month of the |
| 🟠unknown | medium | Server-side pagination | sort_pagination | Paginated list (20/page) with Prev/Next and 'Page X of Y' (StoredSummariesTab.tsx:356,391-393,753,992-1010) | missing | v3 listSummaries takes no page/limit/offset (api.ts:213-223) and loads the whole set, filtering client-side (Summaries.tsx:140-144). Scales poorly for large wor |
| 🟢intentional | medium | Bulk Regenerate over selection | action | Multi-select checkbox per card + BulkActionBar Regenerate(N) with confirm dialog and 'select all matching filters' (BulkActionBar.tsx:212-221,316-344; StoredSum | changed | v3 has no per-row checkboxes; instead a single '↻ Regenerate N' over the filtered set with window.confirm (Summaries.tsx:257-274). ADR-133 D3 marks bulk regener |
| 🟠unknown | medium | Bulk Delete over selection | action | BulkActionBar Delete(N) with AlertDialog confirmation (BulkActionBar.tsx:246-255,285-314) | missing | v3 offers only per-row delete (Summaries.tsx:371-377); no multi-select bulk delete. Not addressed in ADR-133 §D. Deleting many summaries is one-at-a-time. |
| 🟢intentional | medium | Per-summary Push to Channel | action | 'Push to Channel' action opening PushToChannelModal (SummaryActions.tsx:88-91,143-146; StoredSummariesTab onPush :964) | changed | v3 replaces dedicated push-to-Discord-channel with a generic 'Publish to: <configured destination>' picker (Summaries.tsx:517-537; api.publishSummary :246). No  |
| 🟠unknown | medium | Generation wizard: Summary Length (Brief/Detailed/Comprehensive) | field | Length select feeding summary_length, also drives model selection (Summaries.tsx legacy dialog :956-968; wizard threads summaryLength :219-222) | missing | v3 CreateSummary (now/recurring/past) has no length/perspective/options step — submit() sends only channel + time window (CreateSummary.tsx:81-116). Compose box |
| 🟠unknown | medium | Generation wizard: Perspective + custom prompt template selection | field | Perspective select incl. custom templates, threaded as perspective/prompt_template_id into generate (Summaries.tsx legacy :970-1016; wizard :218,227) | missing | v3 CreateSummary does not let the user pick a perspective or prompt template when generating (CreateSummary.tsx:81-116). Prompts exist as a separate screen (ADR |
| 🔵superseded | low | Source filter (All/Real-time/Scheduled/Archive/Manual/Imported) | filter | Select-driven server-side source filter on the summaries list (SummaryFilters.tsx:215-230) | changed | v3 replaces this with a client-side 'kind' facet (Scheduled/Rolling/Retrospective/Ad-hoc) derived from tags — Summaries.tsx:25-30,238-243. ADR-133 §B records pe |
| 🟢intentional | low | Perspective filter | filter | Perspective select with standard + custom perspectives, drawn from usePerspectiveOptions (SummaryFilters.tsx:382-428) | present | v3 has a perspective facet built from perspectives present in the loaded set (Summaries.tsx:135-138,244-256). ADR-133 §B lists Summaries perspective facet as sh |
| 🟢intentional | low | Calendar / List view toggle | behavior | List vs Calendar view of summaries (Summaries imports SummaryCalendar; StoredSummariesTab viewMode) | present | v3 ships List/Calendar toggle + CalendarView (Summaries.tsx:49,276-303,551-607). ADR-133 §B Calendar view shipped. |
| 🔵superseded | low | Granularity filter (daily/weekly/monthly) | filter | Select-driven granularity filter (SummaryFilters.tsx:272-288) | missing | Subsumed by the v3 'retrospective' kind facet (Summaries.tsx:25-30). ADR-133 §B explicitly folds rolling/granularity facets into the kind facet 'without a per-s |
| 🔵superseded | low | Continuity filter (Has Chain / No Chain) | filter | Select filter on continuity chain (SummaryFilters.tsx:291-309) | missing | ADR-133 §B groups Continuity under the kind facet; explicitly chosen as the equivalent over a dedicated continuity filter. |
| 🔵superseded | low | Rolling status filter (Still Rolling / Finalized / All Rolling) | filter | Select filter on rolling status (SummaryFilters.tsx:312-331) | changed | v3 has a 'rolling' kind facet (tags rolling-*) but not the still-rolling/finalized distinction (Summaries.tsx:25-26). ADR-133 §B treats rolling as a kind facet. |
| 🟢intentional | low | Length filter (Brief/Detailed/Comprehensive) | filter | Summary length select filter (SummaryFilters.tsx:362-379) | missing | ADR-133 explicitly lists 'per-summary Length filtering' as a live-only follow-up deferral (status line, §B). |
| 🟠unknown | low | Schedule filter | filter | Filter by originating schedule, dropdown of schedules (SummaryFilters.tsx:333-360) | missing | No v3 equivalent and not called out in ADR-133. Low value; relies on per-summary schedule linkage. Restore as a tag/facet if schedule provenance is stored on su |
| 🟠unknown | low | Scope-type filter (Server-wide/Category/Single Channel) and Channel-mode (single/multi) | filter | Scope filter (SummaryFilters.tsx:234-250) and Channel Mode in More-filters (SummaryFilters.tsx:534-554) | missing | v3 derives a scopeLabel for display (Summaries.tsx:14-20) but offers no scope filter. Not enumerated in ADR-133. Could be added as a facet over channel_id/categ |
| 🟠unknown | low | Platform filter (Discord/Slack/WhatsApp) | filter | Platform select filter (SummaryFilters.tsx:253-269) | missing | No v3 platform facet on the list. Not in ADR-133 §B Summaries list. Multi-platform summaries are otherwise supported via CreateSummary; filtering the stored lis |
| 🟠unknown | low | Sort controls (Created/Message count/Archive period + asc/desc) | sort_pagination | Sort-by select + order toggle (SummaryFilters.tsx:476-507) | missing | v3 list is server-default-ordered with no user sort control; new items prepend via SSE (Summaries.tsx:98-107). No sort affordance. |
| 🟠unknown | low | Content filters: hasKeyPoints/hasActionItems/hasParticipants/grounding/access-issues/private-channels | filter | More-filters popover toggles for content presence, data quality, channel access, privacy (SummaryFilters.tsx:556-700) | missing | v3 has none of these toggles. Grounding is shown per-card (Summaries.tsx:321-331) but not filterable. Not enumerated in ADR-133. |
| 🟠unknown | low | Content-count range filters (message count, key points, action items, participants min/max) | filter | Numeric min/max range inputs in More-filters (SummaryFilters.tsx:702-836) | missing | No v3 equivalent. Niche power-user filters; not in ADR-133. |
| 🟠unknown | low | Active-filter chips with individual remove + filter count badge | behavior | Removable badges per active filter and a count badge on the Filters button (SummaryFilters.tsx:121-150,870-1124) | changed | v3 has a single 'Clear filters' link for q/participant/tag/archived (Summaries.tsx:212-226) but no per-filter chips or active-count badge. |
| 🟢intentional | low | Bulk Confluence Publish / Unpublish | action | BulkActionBar Publish/Unpublish to Confluence with force / delete-pages options (BulkActionBar.tsx:222-245,346-440) | changed | ADR-133 D2 ships bulk publish via POST /destinations/:id/publish {summary_ids}; v3 UI exposes only per-summary 'Publish to:' destination picker (Summaries.tsx:5 |
| 🟠unknown | low | Per-summary Push to DM | action | 'Push to DM' action opening PushToDMModal (SummaryActions.tsx:92-95; StoredSummariesTab onPushDM :965) | missing | No Discord-DM push in v3 (no client method, no card action). Not enumerated in ADR-133 §D. Discord DM destination is an ADR-047 v2 feature. |
| 🟠unknown | low | Per-summary Send to Email | action | 'Send to Email' action opening SendToEmailModal (SummaryActions.tsx:96-99; StoredSummariesTab onEmail :968) | missing | No ad-hoc email-send of a single summary in v3. Email exists only as a configured delivery destination. Not addressed in ADR-133 §D. |
| 🟢intentional | low | Per-summary Push to Drive | action | 'Push to Drive' action when server sync enabled (SummaryActions.tsx:100-105,167-172; StoredSummariesTab onPushToDrive :966) | changed | v3 routes Drive through the generic publish-to-destination picker (ADR-133 D2 'Push to Google Drive'); no dedicated per-card 'Push to Drive' button. Functionall |
| 🟠unknown | low | Generation wizard: Custom date range for on-demand summary | field | Custom start/end date pickers with tooltip + presets, building a custom time_range (Summaries.tsx legacy :845-954; handleGenerate :475-495) | changed | v3 'Now' offers only fixed presets (4h/24h/7d/30d) and 'Past' is whole-history by-week for WhatsApp only (CreateSummary.tsx:20-25,299-303). No arbitrary custom  |
| 🟢intentional | low | Generation wizard: explicit scope selector (Channel/Category/Server) | field | Three-way scope buttons; category select; server-wide info (Summaries.tsx legacy :648-843) | changed | v3 CreateSummary picks a single channel, 'All channels in workspace', or 'All channels in <category>' radios (CreateSummary.tsx:184-213). Covers channel/categor |
| 🟠unknown | low | Generation progress card with parameters + 'View in Jobs' link | loading_state | Inline progress card showing scope/channel-count/timeRange/perspective and link to Jobs while task runs (Summaries.tsx:1040-1071) | changed | v3 CreateSummary shows a 'Working…' button state and a final result string (CreateSummary.tsx:309-318); v3 Summaries shows a 'Summarizing…' button on compose. N |
| 🟠unknown | low | Empty-state guidance copy | empty_state | Archive icon + 'No stored summaries' + actionable hint ('Create a schedule with Dashboard destination…') (StoredSummariesTab.tsx:888-903) | changed | v3 empty state is a terse 'No summaries yet.' / 'No summaries match the filters.' (Summaries.tsx:291-294) with no guidance on how to populate. Lower-fidelity on |
| 🟠unknown | low | Loading skeleton / error state on list | loading_state | StoredSummariesSkeleton while loading and a dedicated isError branch with refetch (StoredSummariesTab.tsx:725-748,2313-2325) | missing | v3 Summaries shows no loading skeleton and no explicit list error/retry state — items just appear when the promise resolves (Summaries.tsx:51-73). On fetch fail |
| 🟠unknown | low | Recency grouping (Today / Last 3 Days / This Week / Older) | behavior | Summary list grouped by recency with section headers and counts (StoredSummariesTab.tsx:941-989) | missing | v3 renders a flat list with no recency grouping (Summaries.tsx:304-543). Calendar view is offered instead. Cosmetic-ish but affects scan-ability of long lists. |

### Tenants → Split across Members.tsx (members/admins/invites) + Settings.tsx (tenant provisioning/selection, LLM config, budget); branding accent only auto-applied in App.tsx  (10 gaps)

_Core tenant identity, RBAC, and provisioning survive (intentionally split into Members + Settings per ADR-133), but the entire tenant-admin surface — create-tenant form with access modes, custom-domain verification, branding editor, and workspace-linking — is absent in v3 with no covering ADR._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🟠unknown | high | Access Mode setting (public / authenticated / members_only / workspace_members) + allowed_email_domains | field | Settings tab Access Mode Select with 4 modes plus a comma-separated Allowed Email Domains input gating 'authenticated' mode, with inline help copy (Tenants.tsx: | missing | No v3 view exposes access_mode or allowed_email_domains. The v3 Tenant type (types.ts:11-16) doesn't even carry these fields. Not listed as a deferral in ADR-13 |
| 🔵superseded | medium | Create Tenant dialog (name + auto-generated slug + subdomain + access_mode picker) | screen | Dedicated CreateTenantDialog with Name, slug (auto-derived from name, URL-safe sanitized), Subdomain (with .summarybot.app suffix), and Access Mode select; open | changed | v3 replaces this with a bare 'claim/create tenant by id' button in Settings.tsx:258-265 (provisionTenant by raw id only — no name, no subdomain, no access_mode) |
| 🟠unknown | medium | Custom domain verification flow (TXT + CNAME records, verify button, status badges) | screen | TenantDomainTab: set custom domain, initiate verification, show DNS TXT verification_record + CNAME target with copy-to-clipboard, a 'Verify Domain' action, plu | missing | v3 keeps subdomain/custom_domain on the Tenant type (types.ts:14-15) and reads them for branding resolution (App.tsx:99-104) but offers no UI to set or verify a |
| 🔵superseded | medium | Branding editor (logo URL + preview, primary color picker, app-name override, show-powered-by toggle) | screen | TenantBrandingTab lets admins set logo_url (with live img preview), primary_color (color picker + hex input), app_name_override (with help text 'Replaces Summar | changed | v3 has no branding editor; it only auto-applies a derived accent color and tenant name at runtime (App.tsx:91-108, applyTenantAccent / setBrand). Logo, explicit |
| 🟠unknown | medium | Link Workspace tab (attach Discord guild to tenant, list/unlink linked workspaces) | screen | TenantWorkspacesTab: select an available (not-yet-linked) Discord guild, optional display name, Link Workspace action, and a linked-workspaces list with guild i | missing | No v3 view links workspaces/guilds to a tenant. ADR-133 claims Tenancy as a v3 strength but lists no workspace-linking deferral. The v3 dashboard is workspace-s |
| 🟠unknown | medium | Delete Tenant danger zone with typed confirmation dialog | action | TenantDetails Danger Zone card with a destructive 'Delete Tenant' button opening a confirmation Dialog ('This will permanently delete the tenant, unlink all wor | missing | No v3 view offers tenant deletion. ADR-133 does not record this as a deferral. Settings supports provision/claim but not delete. Restore: DELETE /tenants/:slug  |
| 🔵superseded | low | Tenant card list view with grid, per-tenant badges, and click-to-detail navigation | navigation | TenantCard grid (sm:2 / lg:3 cols) showing name, /slug, domain badge, access-mode badge, and an unverified-domain badge; clicking a card opens TenantDetails; a  | changed | v3 has no tenant browse/grid. Settings.tsx:217-241 shows 'Your tenants' as small role-labeled pills (listMyTenants, TEN-001) that switch the active tenant id ra |
| 🔵superseded | low | Members: email display, pending-invite badge, and named access levels (viewer/contributor) | field | TenantMembersTab lists members by email (falling back to user_id) with access-level badge and a 'Pending' badge when not accepted; invite form offers 'Viewer (r | changed | v3 Members.tsx lists members by raw user_id only (no email; Members.tsx:120-122) and uses roles member/admin/owner (Members.tsx:10) instead of viewer/contributo |
| 🔵superseded | low | Separate Administrators section with role badges and owner-protected removal | screen | TenantMembersTab renders a dedicated Administrators card (Shield icon) listing admins with role badges, hiding the remove (X) button when role === 'owner' to pr | changed | v3 folds admins into the single members list with a role <select> (member/admin/owner) and an always-present remove button (Members.tsx:123-140); there is no ow |
| 🔵superseded | low | Per-tab item-count badges and consistent empty states | empty_state | TenantDetails tabs show live count badges for Workspaces and Members (Tenants.tsx:489-502); each tab has tailored empty copy ('No workspaces linked yet', 'No me | changed | v3 has no tabbed tenant detail, so count badges are gone; Members has 'No members.' (Members.tsx:115) but no count badge. Minor informational regression followi |

### Webhooks → Delivery  (6 gaps)

_Core parity met: v2 Webhooks is intentionally folded into v3 Delivery (ADR-133 A5) as a plugin-backed destination row-type, preserving create/test/remove/last-status. Genuine gaps are the missing per-row enable toggle and edit affordance, no user-supplied destination name, and weaker empty/loading states._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔵superseded | medium | Per-destination enable/disable toggle (Switch) | action | Each webhook card has a Switch bound to onToggle that PATCHes enabled state inline (Webhooks.tsx:444-447, handler 94-107). | missing | v3 Delivery only renders enabled state read-only ('disabled' label, Delivery.tsx:179) with no toggle; offers only Test + Remove (196-208). The `enabled` field e |
| 🔵superseded | medium | Edit destination (name/url/type) | action | Pencil per-row opens an Edit dialog letting user change name, URL (leave-empty-keeps-current), and type; saves only changed fields (Webhooks.tsx:463-470, dialog | missing | v3 Delivery supports only add (form) and remove; no edit affordance (Delivery.tsx:195-208). Editing requires delete+recreate. Aligns with ADR-133 A5 deferral (' |
| 🔵superseded | low | User-supplied destination Name | field | Create/edit forms require a free-text Name (e.g. 'Slack Notifications') displayed as the card heading (Webhooks.tsx:218-225, 413). | missing | v3 destinations are identified by plugin kind badge + non-secret config hint (Delivery.tsx:172-178); no name field in the add form or Destination type (types.ts |
| 🟠unknown | low | Loading skeleton state | loading_state | While webhooks load, renders WebhooksSkeleton (two skeleton cards) instead of content (Webhooks.tsx:344-345, 504-529). | missing | v3 Delivery has no loading indicator; load() runs in useEffect and the list/form simply renders empty until data arrives (Delivery.tsx:22-37). Restore: show a s |
| 🟢intentional | low | Empty-state with icon, heading, and CTA | empty_state | When no webhooks exist, shows a centered icon, 'No webhooks yet' heading, helper copy, and an 'Add Webhook' button (Webhooks.tsx:346-361). | changed | v3 shows a single muted line 'No external destinations yet — summaries go to the dashboard only.' (Delivery.tsx:165-168) and a separate note when no plugins are |
| 🟢intentional | low | Webhook type selector (fixed enum) | field | Type Select offers a fixed enum: Discord, Slack, Notion, Generic (Webhooks.tsx:239-252, 310-323). | changed | v3 replaces the hardcoded enum with a Type dropdown populated dynamically from build-compiled sink plugins, and renders fields from each plugin's config schema  |

### WhatsAppImports → Whatsapp  (11 gaps)

_v3 Whatsapp covers upload + coverage/gap solicitation (with extra summarize actions) but drops the entire v2 import-management surface: import list with per-item view/delete, sanitized message viewer with search/pagination, and participant rename/merge UI. ADR-121 supersedes ADR-081 and reframes the page around coverage, but it still mandates a sanitized message view and reversible no-silent-merge participant claims, neither of which v3 surfaces._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔴regression | high | Participant identity management: rename (set preferred name) and merge | action | ParticipantList lists per-chat participants with message_count and alias_count, a 'Set name' inline editor (updateParticipantName) and a Merge mode with no-sile | missing | ADR-121 §3 (lines 105-108, 176-180) mandates participant claim/merge with 'no silent merge', reversible and audit-logged — a core ADR-121 requirement (WSP-010). |
| 🔴regression | medium | Sanitized message viewer with search and pagination | screen | MessageList fetches messages (50/page), renders sender/timestamp/content with system-message and attachment styling, a 'Search messages...' box, and Previous/Ne | missing | ADR-121:73 explicitly retains 'a sanitized message view (pseudonyms only, never phone numbers)' as part of the model, yet v3 surfaces no message-browsing UI at  |
| 🔴regression | low | Loading skeleton state for the page | loading_state | While imports load, shows a header Skeleton plus a 3-card skeleton grid (WhatsAppImports.tsx:813-824) | missing | v3 CoverageOverview returns null while chats is null (Whatsapp.tsx:232) — no loading indicator for the coverage section; upload form is always visible so impact |
| 🔵superseded | medium | Import list with per-import cards (filename, status badge, message/participant counts, date range, importer, error) | screen | Renders an 'Imports' card listing each WhatsAppImport via ImportCard with filename, StatusBadge (pending/processing/completed/failed), message_count, participan | missing | ADR-121 supersedes ADR-081 and reframes the page around coverage rather than per-import management; v3 only echoes the single most-recent import result (Whatsap |
| 🔵superseded | medium | View import detail dialog (Messages / Participants / Details tabs) | screen | 'View' button opens a Dialog with three tabs (messages, participants, details) showing format, file size, date range, counts, and a participant summary (WhatsAp | missing | ADR-121 (supersedes ADR-081) drops the import-management UI in favor of coverage. No detail/inspect affordance exists in v3. |
| 🔵superseded | low | Delete import with confirmation dialog | action | Per-import trash button opens a 'Delete Import' confirmation Dialog ('This action cannot be undone') wired to deleteImport (WhatsAppImports.tsx:411-414, 1052-10 | missing | ADR-121 supersedes ADR-081; import deletion UI not carried forward. v3 has no delete affordance. |
| 🔵superseded | low | Chat filter selector (filter imports by chat) | filter | A Filter-icon Select lets the user scope the imports list to 'All chats' or a specific chat ('{name} ({n} imports)') via selectedChatId (WhatsAppImports.tsx:842 | missing | v3 lists all chat coverage cards unconditionally (Whatsapp.tsx:240-247) with no filter; acceptable given the import list itself is gone, but the per-chat scopin |
| 🟢intentional | low | Drag-and-drop upload dropzone with file-type acceptance and drag states | behavior | UploadDropzone uses react-dropzone with accept .txt/.zip, isDragActive states ('Drop the file here'), and an uploading state (WhatsAppImports.tsx:292-347) | changed | v3 uses a plain file input with accept='.zip,.txt' (Whatsapp.tsx:98-103). Functionally equivalent file selection; only the drag-drop affordance is lost. Cosmeti |
| 🟢intentional | low | Google Drive import button | action | DriveUploadButton lets the user import exports from a Google Drive folder (ADR-082) with toast feedback (WhatsAppImports.tsx:877-895) | missing | Explicitly deferred by ADR-121:215-216: 'Direct upload is v1; Google-Drive-sourced uploads (reference ADR-082) are deferred.' |
| 🔵superseded | low | Refresh button to re-fetch imports | action | Header 'Refresh' button calls refetch() to reload the imports list (WhatsAppImports.tsx:836-839) | changed | v3 has no manual refresh; coverage reloads automatically on mount and after import (Whatsapp.tsx:35-37, 52). Acceptable since the imports list it refreshed no l |
| 🟢intentional | low | Per-chat coverage % badge and gap detail on chat cards | field | ChatCard shows a 'X% coverage' badge, import/message counts, coverage date range, and up to 2 gaps with pre_join_context vs gap styling plus a '+N more gaps' ro | present | Satisfied by equivalent: v3 ChatCoverageCard renders a visual covered/gap timeline bar, ~X% covered text, counts, fillable-gap list and contributors (Whatsapp.t |

### guild dashboard home) → Overview  (10 gaps)

_v3 Overview covers the same intent (ADR-133 A6: read-only aggregate home) and adds Coverage/Members/Spend cards, but drops the loading skeleton, the "This Week" and "Enabled Channels" stat cards, the prominent Generate Summary CTA, the Quick Actions panel (Configure Channels / Manage Schedules / Import WhatsApp), and shows a flat recent-summaries list without the per-stat error-breakdown badge panel._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔴regression | medium | Primary "Generate Summary" CTA button | action | Prominent header button linking to the summaries/generate flow with Sparkles icon. GuildDashboard.tsx:71-76 | missing | v3 header has only title + subtitle (Overview.tsx:42-46); no create-summary affordance. The CreateSummary view exists but is not linked from Overview. Restore:  |
| 🔴regression | low | Loading skeleton (DashboardSkeleton) | loading_state | While the guild is loading, v2 renders a full skeleton matching the layout (header + 4 stat cards). GuildDashboard.tsx:15-17, 250-277 | missing | v3 shows nothing until data arrives (data && ... at Overview.tsx:48); no skeleton or spinner. Restore: render a lightweight skeleton/loading placeholder when da |
| 🟠unknown | medium | "This Week" stat card (summaries_this_week) | field | Stat card showing guild.stats.summaries_this_week. GuildDashboard.tsx:35-41 | missing | v3 Overview type has no this-week count (types.ts:309-318). ADR-133 A6 lists 'counts (summaries, schedules, members)' but not a weekly count; not explicitly def |
| 🟠unknown | medium | "Enabled Channels" stat card | field | Stat card showing guild.config.enabled_channels.length — config status surfaced on the dashboard. GuildDashboard.tsx:49-55 | missing | ADR-133 A6 explicitly scopes Overview to include 'config status', but v3 surfaces no channel/config count. v3 substitutes Coverage% (Overview.tsx:53). Restore:  |
| 🟠unknown | medium | Quick Actions: Configure Channels link | navigation | Quick-action card linking to channels config with descriptive copy 'Select which channels to summarize'. GuildDashboard.tsx:157-171 | missing | v3 has no Quick Actions panel at all. ADR-133 A6 says Overview is 'mostly composition of existing data' (read-only) — quick-action navigation shortcuts are not  |
| 🟠unknown | medium | Quick Actions: Import WhatsApp Chat link | navigation | Highlighted quick-action card (green) linking to archive/import with Upload icon and copy 'Upload a WhatsApp export zip file'. GuildDashboard.tsx:189-203 | missing | v3 has a dedicated Whatsapp view but Overview provides no entry point to it. ADR-133 reorganizes WhatsApp under Sources, but the import entry point is not surfa |
| 🟠unknown | low | Quick Actions: Manage Schedules link | navigation | Quick-action card linking to schedules with copy 'Set up automated summary schedules'. GuildDashboard.tsx:173-187 | missing | Schedules count card in v3 is clickable (Overview.tsx:52) and navigates to schedules, partially covering this. Lower severity since the nav target is reachable  |
| 🔵superseded | low | Error breakdown panel (per-type counts, last 24h) | field | When errorCounts.total>0, renders an Errors(Last 24h) card with per-type badges (type:count) plus a 'N total' badge and a 'View All' link to errors. GuildDashbo | changed | v3 collapses this to a single 'Open errors' count card (unresolved_errors) that turns red and navigates to errors (Overview.tsx:56-61). Per-type breakdown and t |
| 🔵superseded | low | Recent summaries titles + 'last summary at' detail | field | Recent panel shows empty state 'No summaries yet. Generate your first one!' or, when summaries exist, 'Last summary: <localized date>'. GuildDashboard.tsx:225-2 | changed | v3 instead lists actual recent summary rows (title + created_at) which is richer than v2's single last-summary line (Overview.tsx:71-84). Empty state copy diffe |
| 🟢intentional | low | Recent summary rows are not clickable | navigation | v2 had no per-row links either (only a last-summary line), so not a v2 affordance. GuildDashboard.tsx:233-240 | present | No gap — noted for completeness: v3 recent rows (Overview.tsx:75-82) are display-only, consistent with v2. |

### server info) → /workspaces/summarybot-3/web/src/views/Settings.tsx (per-tenant LLM provider + budget + per-workspace summary instructions)  (11 gaps)

_Same screen name, almost entirely different content: v3 Settings is tenant LLM/budget config while v2 Settings is Discord guild config; most v2 elements are either superseded by the tenancy/Prompts model, relocated to other v3 views (Prompts, Plugins, Source, Overview), or deferred. Several real gaps remain (default summary-length/perspective/toggles, timezone selector, channel sensitivity, server build info, save-state UX)._

| Cls | Sev | Element | Kind | v2 behavior | v3 | Note |
|---|---|---|---|---|---|---|
| 🔴regression | medium | Timezone selector (grouped Americas/Europe/Asia/Oceania/UTC) with live 'Current time' preview, persisted via TimezoneContext for all dashboard date display. | field | Grouped Select over TIMEZONE_OPTIONS bound to TimezoneContext; shows current time in chosen tz. v2 Settings.tsx:200-242. | missing | v3 Settings has no global dashboard timezone preference. Timezone exists only as a per-action input (CreateSummary.tsx:99, Schedules.tsx:57, Whatsapp.tsx auto-d |
| 🔵superseded | medium | Default Summary Options: Summary Length select (brief / detailed / comprehensive, each with a helper sub-label) and Perspective select (general/developer/marketing/executive/support), persisted as guild default_options and seeded on load. | field | Two Select controls bound to options.summary_length and options.perspective with rich item descriptions; saved via updateConfig({default_options}). v2 Settings. | missing | v3 has no per-workspace default summary length/perspective on Settings. Perspectives moved to the dedicated Prompts/Perspectives view (web/src/views/Prompts.tsx |
| 🟠unknown | medium | Channel Sensitivity Settings (ADR-046): mark channels/categories sensitive so their summaries require admin role to view; includes search, per-channel toggles, category bulk, locked/restricted badges. | screen | <ChannelSensitivitySettings guildId channels categories/> rendered on Settings. v2 Settings.tsx:244-252; component ChannelSensitivitySettings.tsx:1-7,40+ (searc | missing | No channel-sensitivity / per-channel access-gating UI found in v3 Settings or Source.tsx (Source only lists/syncs channels, no sensitivity flag). Not listed in  |
| 🔵superseded | low | Include Action Items and Include Technical Terms toggles (boolean default options with explanatory copy). | default | Two Switch rows writing options.include_action_items / include_technical_terms into the guild default_options. v2 Settings.tsx:169-195. | missing | v3 has no workspace-level toggle to opt in/out of action items or technical terms; both are produced and shown per-summary instead (Summaries.tsx:413-417 action |
| 🟢intentional | low | Google Drive Sync settings card (per-tenant OAuth Drive config: shared-drive selection, folder structure, JSON toggle). | screen | <GoogleDriveSyncSettings guildId/> on Settings; per-server Drive OAuth + export config (ADR-007/091). v2 Settings.tsx:254-260; GoogleDriveSyncSettings.tsx:1-30. | changed | Relocated, not lost. v3 handles Drive as a delivery destination/plugin with OAuth Connect wizards (Plugins.tsx:157 'Connecting via OAuth (Google Drive / Conflue |
| 🟢intentional | low | Confluence integration settings card (per-tenant Confluence connect/test/publish config, ADR-099). | screen | <ConfluenceSettings guildId/> on Settings with connect/test-connection/delete. v2 Settings.tsx:262-268; ConfluenceSettings.tsx:1-30 (test connection, dialog). | changed | Relocated to v3 delivery plugins / Connect wizard (Plugins.tsx:157, Delivery.tsx; per-summary publish in ADR-133 §D2). ADR-133 §C credits Drive/Confluence OAuth |
| 🟢intentional | low | Prompt Configuration card: custom prompt GitHub repository input + Sync button (disabled, 'Coming soon') and default-prompts status row. | screen | Disabled GitHub-repo input + Sync button, 'Coming soon - Configure via Discord /config prompts repo:URL'. v2 Settings.tsx:270-327. | changed | v2's own control was a disabled 'Coming soon' stub, so no real functionality was lost. v3 ships a working Prompts/Perspectives view instead (Prompts.tsx, ADR-13 |
| 🔵superseded | low | Discord Server info card: Server ID, Member Count, total Channels, Enabled Channels count. | screen | Read-only dl of guild.id, member_count, channels.length, config.enabled_channels.length. v2 Settings.tsx:329-360. | changed | v3 is workspace/tenant-centric, not Discord-guild-centric, so 'Server ID / member count / enabled channels' has no direct analogue on Settings. Workspace/source |
| 🟠unknown | low | Server Info card: app Version, Build hash, and relative Built date (from /health). | screen | Read-only Version/Build/Build-date from useHealth + formatRelativeTime. v2 Settings.tsx:362-396. | missing | No build/version/build-date panel found in v3 Settings or other audited views. Not addressed in ADR-133. Minor operability affordance (knowing which build is de |
| 🟢intentional | low | Save-changes UX: dirty-state tracking (hasChanges) that reveals a single 'Save Changes' button with spinner, plus success/error toasts. | behavior | Save button only appears when hasChanges; shows Loader2 while pending; toast on success/failure. v2 Settings.tsx:42,69-72,91-100,55-66. | changed | v3 uses per-section explicit Save buttons with inline status text instead of a global dirty-state save (e.g. Save instructions Settings.tsx:198-205,57; Save / C |
| 🟠unknown | low | Loading skeleton while guild config loads. | loading_state | SettingsSkeleton renders shaped placeholders during isLoading. v2 Settings.tsx:74-76,401-428. | missing | v3 Settings shows no skeleton/loading placeholder; sections render once fetches resolve (e.g. summary_instructions effect Settings.tsx:27-33, LLM config only af |
